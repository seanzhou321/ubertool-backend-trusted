# SBR Requirements Traceability Matrix: 006-tools-image-storage

**Source Spec**: `specs/006-tools-image-storage/spec.md`
**Adapter**: `sbr/README.md` (This repo's adapter)
**Generated**: 2026-07-29
**Mode**: retrofit audit

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding | Boundary Status | Planned Tests | Notes |
|---|---|---|---|---|---|---|---|---|
| FR-001 | `UpdateTool` and `DeleteTool` MUST require the caller to be the tool's `owner_id`, verified by a fresh DB read before any write. | `TestToolService_UpdateDelete_RequiresOwnership` (tests/unit/tool_service_test.go:36) — "UpdateTool rejects a non-owner caller"/"...accepts the actual owner" (41,54), "DeleteTool rejects..."/"...accepts..." (68,81); confirmed each subtest asserts `repo.GetByID` runs before any write and non-owner calls are rejected | — | `TestToolService_E2E` > "UpdateTool and DeleteTool reject a non-owner caller" (tests/e2e/tool_test.go:193-217) — real gRPC handler + live DB | Same e2e journey (no tools-table smoke test exists) | Complete | — | No L2: ownership logic lives in the service, not the repository; `tests/integration/tool_test.go` only covers Create/GetByID/ListByOrg. |
| FR-002 | `AddTool` MUST set `owner_id` from the caller's JWT, never from the request body. | `TestToolHandler_AddTool` > "Sets owner_id from the caller's JWT-derived user ID" (tests/unit/handlers/tool_handler_test.go:49-65) — loops two caller IDs via `metadata.Pairs("user-id", ...)`, asserts `tool.OwnerID` matches the caller, not any request field | — | `TestToolService_E2E` > "AddTool Workflow" (tests/e2e/tool_test.go:23-65, lines 59-64 comment-cite FR-002) — queries DB directly for `owner_id`, asserts it equals the caller | Same e2e journey | Complete | — | The relevant logic lives in the **handler** (`internal/api/grpc/tool.go:20-38`), not the service — L1 evidence correctly comes from `tests/unit/handlers/`, not `tests/unit/tool_service_test.go`. |
| FR-003 | `SearchTools` MUST resolve metro from `organization_id` when given, require explicit `metro` otherwise, require non-empty `query`, and filter out results whose owner shares no org with the caller. | `TestToolService_SearchTools` (tests/unit/tool_service_test.go) — "Success" (204,211), "SharedOrgFiltering_ReturnsToolWhenUsersShareOrg" (237), "SharedOrgFiltering_FiltersOutToolWhenNoSharedOrg" (261), "Rejects an empty query" (288), "Rejects an empty metro when organization_id is not specified" (300) | — | `TestToolService_E2E` > "SearchTools with Org Membership Verification" (tests/e2e/tool_test.go:67-108) — covers org-membership rejection only | — | Gap — L3 (shared-org filter, empty-query/metro rejection) | L3: **fix, don't write from scratch** — `tests/e2e/search_tools_shared_org_test.go_` (trailing underscore) already contains `TestSearchTools_SharedOrgFiltering` covering exactly this gap; rename to `.go`, confirm it still compiles/passes against current code, and re-run this audit to promote it from "planned" to real evidence. If it no longer passes, fix it rather than writing a new test from scratch. Separately add an e2e subtest for the empty-query/empty-metro rejection paths if the rename doesn't already cover them. | **`tests/e2e/search_tools_shared_org_test.go_` (trailing underscore) contains real subtests for the shared-org filter but is excluded from the Go build** — it does not compile or run, so it is not executable evidence. spec.md's own Known Discrepancy 3 already states this correctly. Metro-resolution-via-org-membership is covered at e2e; the shared-org filter and empty-query/metro rejections are not. |
| FR-004 | `GetUploadUrl`, `ConfirmImageUpload`, `DeleteImage`, `SetPrimaryImage` MUST require the caller to own the target tool (or, for `ConfirmImageUpload`, the pending image record). | `TestImageStorageService_OwnershipChecks` (tests/unit/image_storage_service_test.go:30) — `ConfirmImageUpload` reject/accept (37,47), `DeleteImage` reject/accept (62,93), `SetPrimaryImage` reject/accept (104,114). **`GetUploadUrl` has zero unit tests** (grepped, zero hits), though its ownership check exists in code (`internal/service/image_storage.go:50-57`) | — | `TestImageStorageService_E2E` > "Upload Image - Unauthorized User" (tests/e2e/image_storage_test.go:370-390) covers `GetUploadUrl`'s rejection — the one RPC missing at L1. `ConfirmImageUpload`/`DeleteImage`/`SetPrimaryImage` only have **happy-path** e2e subtests, no non-owner-rejection e2e coverage | — | Gap — L1 (`GetUploadUrl`), L3 (rejection paths for the other 3) | L1: `TestImageStorageService_GetUploadUrl_RequiresOwnership` (new, tests/unit/image_storage_service_test.go) — mirror the existing `OwnershipChecks` pattern for `GetUploadUrl`. L3: `TestImageStorageService_E2E` > "ConfirmImageUpload/DeleteImage/SetPrimaryImage reject a non-owner caller" (new subtests, tests/e2e/image_storage_test.go). | The four RPCs' ownership checks are cross-covered (each has evidence somewhere) but no single RPC has full unit+e2e rejection coverage. Not a clean "all four verified" Complete. |
| FR-005 | `GetDownloadUrl` MUST grant access to the tool's owner or, for any other caller, only when the tool's status is `AVAILABLE`. | `TestImageStorageService_GetDownloadUrl` (tests/unit/image_storage_service_test.go:129) — "Owner can always download" (137), "Non-owner can download when the tool is AVAILABLE" (147), "Non-owner is rejected when the tool is not AVAILABLE" (157) — directly exercises the disjunctive boundary | — | `TestImageStorageService_E2E` > "Upload and Download Image via Presigned URL" (120-139), "Download Thumbnail" (312) — both only exercise the **owner** path | — | Gap — L3 | L3: `TestImageStorageService_E2E` > "GetDownloadUrl grants non-owner access only when tool is AVAILABLE, rejects otherwise" (new, tests/e2e/image_storage_test.go) — mirror the L1 subtests through the real gRPC handler + live DB. | The owner-vs-AVAILABLE disjunctive boundary is unit-verified but has no e2e test with a non-owner caller against either an AVAILABLE or non-AVAILABLE tool. |
| FR-006 | Thumbnail generation MUST run asynchronously after `ConfirmImageUpload` returns, and MUST NOT block or fail the RPC if generation fails. | `TestImageStorageService_OwnershipChecks` > "ConfirmImageUpload accepts the actual owner" (tests/unit/image_storage_service_test.go:47-60, esp. line 55: forces `ReadFile` to error, asserts `ConfirmImageUpload` still returns `CONFIRMED` with no error) | — | `TestImageStorageService_E2E` > "Upload and Download Image via Presigned URL" (tests/e2e/image_storage_test.go:98-118) — polls `thumbnail_path` via `require.Eventually` after the RPC has already returned, verifies a real JPEG is produced | Same e2e journey | Complete | — | Both asynchronicity and failure-tolerance are directly verified. |
| FR-007 | `GetToolImages` MUST NOT be assumed to enforce the same access rule as `GetDownloadUrl` (Known Discrepancy 1) — target correctness bar for follow-up work, not current behavior. | — | — | `TestImageStorageService_E2E` > "GetToolImages does not restrict access to the owner (documents current non-enforcement)" (tests/e2e/image_storage_test.go:192-217) — a caller unrelated to the tool succeeds, locking in the absence of access control as a regression baseline | — | Complete *(as a documented-gap baseline)* | N/A — the correctness bar itself (matching `GetDownloadUrl`'s rule) is an acknowledged missing-feature gap per spec.md, not a missing-test gap; a test enforcing access control would have nothing to assert until that access control is actually implemented. Route to a feature task, not a test stub. | The e2e test proves exactly the FR's own claim — that no access control is enforced. Do **not** read this as "access control is verified" — the underlying correctness bar remains an acknowledged, intentional gap for future work, exactly as spec.md frames it. |
| FR-008 *(multi-org, PRD 3.3, Organizations FR-011)* | `SearchTools` MUST include tools from ALL orgs the caller belongs to in the resolved metro — not limited to the `organization_id` supplied. | `TestToolService_GetSharedOrganizations` > "HandlesMultipleOrgs_ReturnsOnlyShared" (tests/unit/tool_service_test.go:370-402) — owner in orgs {1,2,3}, requester in {2,4}, asserts exactly org 2 returned; proves the shared-org check spans full membership on both sides (the mechanism FR-008 depends on) | — | — | — | Gap — L1 (exact scenario), L3 | L1: `TestToolService_SearchTools_CrossOrgInclusion_DifferentSharedOrg` (new, tests/unit/tool_service_test.go) — caller supplies `organization_id=X` (drives metro resolution), while a returned tool's owner shares only a *different* org `Y` with the caller; assert the tool is still included. L3: same rename fix as FR-003/FR-009 below (`search_tools_shared_org_test.go_` → `.go`), plus an e2e subtest for this specific org-driven-metro scenario if not already covered. | Code (`internal/service/tool.go:158-191,266-307`, `internal/repository/postgres/tool.go:135-170`) confirms the repository filters by metro only (no org predicate) and the post-filter checks full membership overlap, matching FR-008's description. But **no test exercises the precise FR-008 scenario**: `organization_id=X` given (driving metro resolution) while a returned tool's owner shares a *different* org `Y` with the caller. All existing "multi-org" subtests use `metro` directly with `organization_id=0`, not org-driven metro resolution combined with cross-org inclusion. |
| FR-009 *(multi-org, PRD 3.3)* | Owner org info in search results MUST be limited to orgs shared with the caller (role-specific: also requires owner not `lending_blocked`, requester not `renting_blocked`). | `TestToolService_GetSharedOrganizations` — "ReturnsSharedOrgs_WhenUsersShareOrg" (316), "ReturnsEmptyOrgs_WhenUsersDoNotShareOrg" (347), "HandlesMultipleOrgs_ReturnsOnlyShared" (370, owner's 3 orgs reduced to the 1 shared org); `TestToolService_GetSharedOrganizations_RespectsBlockedFlags` (tests/unit/tool_service_test.go:406, for the blocked-flag rule); `TestToolService_SearchTools` > "SharedOrgFiltering_ReturnsToolWhenUsersShareOrg" (237) also asserts `res[0].Owner.Orgs` has length 1 with the correct org ID | — | `TestSearchTools_SharedOrgFiltering` > "ReturnsToolWhenUsersShareOrg" exists in the **same disabled** `tests/e2e/search_tools_shared_org_test.go_` as FR-003 (asserts `Owner.Orgs` contains only the shared org) — not executable | — | Complete (L1); Gap — L3 | L3: same rename fix as FR-003 — `tests/e2e/search_tools_shared_org_test.go_` → `.go`, verify it still passes, re-audit to promote from planned to real. | L1 coverage is strong and specific, including the blocked-flag rule. L3 evidence exists in source but doesn't run (same disabled-file issue as FR-003). |

## Summary
- Total FRs: 9
- Complete: 5 (FR-001, FR-002, FR-006, FR-007 — as a documented-gap baseline, FR-009 at L1)
- Gaps: 4 (FR-003, FR-004, FR-005, FR-008), plus FR-009's L3 gap
- Unclassified: 0

**Gaps**: FR-003 (shared-org filter / empty-query-metro rejection only exist in a disabled, non-compiling e2e file — planned fix: re-enable it), FR-004 (`GetUploadUrl` has no unit test; the other 3 RPCs have no e2e rejection-path test — planned tests proposed), FR-005 (owner-vs-AVAILABLE boundary untested at e2e — planned test proposed), FR-008 (the precise org-driven-metro + cross-org-inclusion scenario has no direct test — planned test proposed), FR-009 (same disabled-e2e-file issue as FR-003 for its L3 tier).

`tests/e2e/search_tools_shared_org_test.go_` (trailing underscore in the filename) contains
real, well-targeted e2e subtests for the FR-003/FR-009 shared-org filtering behavior, but the
trailing underscore excludes it from the Go build — it is dead code, not executable evidence,
despite looking like coverage at a glance. Renaming it to `.go` and confirming it still
compiles/passes would close two L3 gaps (FR-003, FR-009) plus contribute to FR-008.
