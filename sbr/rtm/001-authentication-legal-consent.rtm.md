# Requirements Traceability Matrix — Authentication & Legal Consent

- **Source spec**: `specs/001-authentication-legal-consent/spec.md`
- **Tier mapping in effect**: `sbr/README.md` → "This repo's adapter" (L1=`tests/unit`,
  L2=`tests/integration`, L3=`tests/e2e`, Grounding=`tests/smoke`+`tests/e2e` journeys)
- **Generated**: 2026-07-22 by `speckit-sbr-audit`; updated 2026-07-23 (Phase 4 re-audit)
- **Mode**: retrofit audit (as-built) — see project constitution Principle I

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding (Smoke) | Boundary Status | Notes |
|---|---|---|---|---|---|---|---|
| FR-001 | `Login` MUST authenticate against canonical `password_hash`, fall back to `pending_credentials`, and reject with a generic error if neither matches. | `TestAuthService_Login` (`tests/unit/auth_test.go`) — 3 subtests: canonical success, pending-credential fallback success, reject-neither | `Test2FAFlow > "Step1_Login_Should_Return_2FA_Token"` (`tests/integration/2fa_test.go:67`) — canonical-password success path only | `TestAuthService_E2E > "Login Flow - Canonical Password Success"` (`tests/e2e/auth_test.go:151`), `> "Login Flow - Pending Credential Fallback"` (`:230`), `> "Login Flow - Invalid Credentials Rejected"` (`:262`) | `TestDatabaseConnectivity > "UsersTableReachable_via_Login"` (`tests/smoke/smoke_test.go:142`) — liveness probe only | **Complete** | **Fixed 2026-07-22 (L2/L3), reinforced 2026-07-23 (L1, SBR remediation Phase 4).** The e2e "Login Flow" subtest previously set `password_hash` to a non-bcrypt literal, making it an accidental-pass (see git history, this file's prior revision). Replaced with 3 real subtests using genuine bcrypt hashes. The L1 gap closed 2026-07-23: a mocked, service-level unit test now covers all 3 clauses independently of the shared per-IP Login rate limiter (FR-012) that constrains this file's L2/L3 evidence. No bug found. |
| FR-002 | `Login` MUST issue a `2fa_pending` JWT and generate a 2FA code, using the random-code+email path when `two_fa.enabled=true` and the fixed-passcode/no-email path only when `false`. | `TestAuthService_Login_TwoFA_ProductionPath` (`tests/unit/auth_test.go:136`) | `Test2FAFlow > "Step1_Login_Should_Return_2FA_Token"` (`tests/integration/2fa_test.go:67`), `> "Step4_Verify2FA_With_Valid_Code_Should_Succeed"` (`:148`) — `two_fa.enabled=false` path | — | — | **Complete** | Fixed 2026-07-22: added a unit test constructing `authService` with `TwoFAConfig{Enabled: true}` directly, capturing the randomly-generated 5-digit code from the mocked email send, and confirming `Verify2FA` accepts that code (not the fixed test passcode). The `enabled=false` path was already covered at L2. |
| FR-003 | `Verify2FA` MUST require a valid `2fa_pending` token, reject an incorrect/consumed code, and on success issue access+refresh tokens and delete the code. | `TestAuthService_Verify2FA_CodeReuse` (`tests/unit/auth_test.go`) | `Test2FAFlow > "Step2_Verify2FA_With_Invalid_Code_Should_Fail"` (`tests/integration/2fa_test.go:95`), `> "Step3_Verify2FA_Without_Token_Should_Fail"` (`:126`), `> "Step4_Verify2FA_With_Valid_Code_Should_Succeed"` (`:148`) | — (active build) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** Confirms a valid code succeeds once, and a second Verify2FA call with the same (now-consumed) code fails with `ErrInvalid2FACode`. No bug found. |
| FR-004 | `RefreshToken` MUST require a valid, non-expired `refresh`-type token and issue a new access/refresh pair with the same identity claims. | `TestAuthService_RefreshToken` (`tests/unit/auth_test.go`) — 3 subtests: issues a new pair preserving identity claims, rejects a non-refresh token type, rejects an invalid/malformed token | **N/A** — `RefreshToken` has no repository dependency at all (pure JWT validate/regenerate via `security.TokenManager`); there is no service+real-DB boundary for an L2 test to exercise | `TestAuthService_E2E > "Login Flow - Canonical Password Success"` (`tests/e2e/auth_test.go:151`) | — | **Complete** | **Fixed 2026-07-22 (L3), reinforced 2026-07-23 (L1, SBR remediation Phase 4).** `jwt_verification_test.go_` (excluded from the build, Known Discrepancy 6) was renamed into the build; its 2FA code was stale (`"123456"` vs. the config's `"47291"`) and would have failed immediately once compiled. Its 2 subtests were also fully redundant with each other. Consolidated into the "Canonical Password Success" subtest above (folding in JWT claims decoding + refresh assertions) to stay within the shared per-IP Login rate limiter's 3-attempt burst (FR-012) — see `sbr/remediation-plan.md`. The L1 gap closed 2026-07-23 with a DB-free unit test (real `security.TokenManager`, no mocks needed) that doesn't compete for that shared budget. No bug found. |
| FR-005 | `Logout` MUST mark the caller's FCM token(s) `OBSOLETE`; MUST NOT be relied on to invalidate JWTs. | `TestAuthService_Logout` (`tests/unit/auth_test.go`) — 2 subtests: marks tokens obsolete, a MarkObsoleteByDevice failure does not fail logout | `TestAuthService_Logout_Integration` (`tests/integration/auth_test.go`) — real DB: confirms the FCM token row's `status` actually flips to `OBSOLETE` in Postgres, not just that the mocked repo method was called | — | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3, L1; Phase 4, L2).** No bug found in either pass. |
| FR-006 | `UserSignup` MUST require a valid/unused/unexpired invitation matching the email, reject if the user already exists, and on success create the user, mark the invitation used, link any originating join request `JOINED`, and add the user as `MEMBER`. | `TestAuthService_Signup` (`tests/unit/auth_test.go`) — 5 subtests: already-registered reject, expired-invitation reject, used-invitation reject, invalid-invitation-code reject, success-links-originating-join-request-as-JOINED | **Intentionally not separately tested** — see Notes | `TestAuthService_E2E > "Signup with Valid Invitation"` (`tests/e2e/auth_test.go:25`) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** All 4 abuse-path reject clauses and the join-request-linked-as-`JOINED` clause now unit-tested. No bug found — production behavior matched the spec exactly. L2 not added: the e2e subtest already calls `Signup` through the real gRPC/DB stack and directly asserts the resulting `users`/`invitations`/`users_orgs` rows, so a dedicated repo-level L2 test would verify the same real-DB write path a second time with no new information — L3 here subsumes L2 (same reasoning as 008/FR-010's `ListPayments`). |
| FR-007 | `RequestToJoinOrganization` MUST create the `join_requests` row before notifying the admin, verify the org exists, and verify `admin_email` holds `ADMIN`/`SUPER_ADMIN` before notifying. | `TestAuthService_RequestToJoin` (`tests/unit/auth_test.go`) — 3 subtests: success, rejects a non-existent org, persists the join request even when admin verification subsequently fails | **Intentionally not separately tested** — see Notes | `TestAuthService_E2E > "RequestToJoin Organization"` (`tests/e2e/auth_test.go:70`) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** The FR's most distinctive clause — the `join_requests` row persisting even when admin verification subsequently fails (spec.md Edge Cases / US3 Scenario 6) — is now regression-locked. No bug found. L2 not added: the e2e subtest already asserts the real `join_requests` row and the admin's `notifications` row against Postgres directly, so a separate L2 test would duplicate that same real-DB assertion — L3 subsumes L2 here. |
| FR-008 | `ChangePassword` MUST verify the old password (canonical or valid temporary credential) and on success update the canonical hash and stamp any outstanding temporary credential used. | `TestAuthService_ChangePassword` (`tests/unit/auth_test.go:208`) — 3 subtests: canonical-password success, pending-credential-fallback success, wrong-old-password reject | `TestAuthService_ChangePassword_Integration` (`tests/integration/auth_test.go`) — real DB: confirms `users.password_hash` actually verifies against the new password and an outstanding `pending_credentials.used_at` is actually stamped | — | — | **Complete** | Fixed 2026-07-22 (L1), 2026-07-23 (L2, SBR remediation Phase 4). No bug found in either pass — production behavior matched the spec exactly. |
| FR-009 | `ResetPassword` MUST return an identical generic success response regardless of account existence, and only create/email a temporary credential when the account exists. | `TestAuthService_ResetPassword` (`tests/unit/auth_test.go:266`) — 2 subtests | `TestAuthService_ResetPassword_Integration` (`tests/integration/auth_test.go`) — real DB, 2 subtests: an existing account gets a real `pending_credentials` row (correct hash/expiry, `used_at` null), a non-existent email creates no row and returns no error | — | — | **Complete** | **Fixed 2026-07-22 (L1) — real bug found and fixed.** `authService.ResetPassword`'s not-found branch returned a non-nil `error`, which the gRPC handler propagated as a distinguishable error response instead of the same generic success the found-user path returns — a user-enumeration vulnerability defeating this FR's entire purpose. Fixed in `internal/service/auth.go` (not-found branch now returns `nil`); regression-locked by `TestAuthService_ResetPassword/Non-existent_email_returns_no_error`. **L2 added 2026-07-23 (SBR remediation Phase 4)** to confirm the actual DB write against Postgres, given this FR's security history; no additional bug found. |
| FR-010 | `RecordLegalConsent` MUST reject empty `doc_names`/`version` with `INVALID_ARGUMENT` and MUST be idempotent under repeat calls. | `TestAuthService_RecordLegalConsent` (`tests/unit/auth_test.go`) — forwards to the repository, propagates a repository error | **Intentionally not separately tested** — see Notes | `TestAuthService_E2E > "RecordLegalConsent and GetUserConsentStatus" > "RecordLegalConsent rejects empty doc_names"`, `> "... rejects empty version"`, `> "RecordLegalConsent is idempotent and GetUserConsentStatus reflects it"` (`tests/e2e/auth_test.go`) — the `INVALID_ARGUMENT` validation (handler-layer, `internal/api/grpc/auth.go`) and the `ON CONFLICT DO NOTHING` idempotency (DB-level, `internal/repository/postgres/legal_consent.go`) are both real-DB/contract-level guarantees not meaningfully mockable, so e2e is the tier that actually exercises them | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 4).** No bug found. L2 not added: the e2e idempotency subtest already asserts the real `user_legal_consents` row count against Postgres directly (the exact same `ON CONFLICT DO NOTHING` semantic an L2 repo test would check) — L3 subsumes L2 here. |
| FR-011 | `GetUserConsentStatus` MUST reject empty `current_version` with `INVALID_ARGUMENT` and MUST compute `pending_docs` against the fixed `domain.KnownLegalDocs` list. | `TestAuthService_GetUserConsentStatus` (`tests/unit/auth_test.go`) — 3 subtests: all-current when every known doc is consented at the current version, returns docs not yet consented (including a stale-version consent not counting as current), propagates a repository error | **N/A** — this FR's only DB-adjacent behavior is `ListByUser` (a plain read, already exercised via the L1 mock and the L3 real-DB path); the `pending_docs` computation itself is pure in-memory logic against `domain.KnownLegalDocs`, with no distinct real-DB boundary for an L2 test to add | `TestAuthService_E2E > "RecordLegalConsent and GetUserConsentStatus" > "GetUserConsentStatus rejects empty current_version"`, `> "GetUserConsentStatus reports every known doc pending before any consent is recorded"` (`tests/e2e/auth_test.go`) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 4).** No bug found. |
| FR-012 | `Login` and `Verify2FA` MUST be rate-limited per client IP (3-attempt burst, 1 refill/3 min). | — | **N/A** — the rate limiter (`internal/security/rate_limiter.go`) is a purely in-memory, process-lifetime component with no repository/DB dependency at all; there is no service+real-DB boundary for an L2 test to exercise | `TestAuthService_RateLimit_E2E` (`tests/e2e/rate_limit_test.go:30`) — isolated via the `ratelimit` build tag, run via `make test-e2e-rate-limit` | — | **Complete** | Fixed 2026-07-22. Confirmed real and correctly implemented: both Login and Verify2FA independently throttle after exactly 3 attempts. Isolated behind a build tag because it deliberately exhausts the shared, process-lifetime, per-IP limiter that this file's other Login/Verify2FA-calling tests also depend on — see `sbr/remediation-plan.md` for why. |

## Summary

- **12 FR-IDs audited, all 12 fully `Complete`** (FR-001, FR-002, FR-004, FR-006, FR-008,
  FR-009, FR-012 — FR-006 closed 2026-07-23 as SBR remediation Phase 2, rest closed 2026-07-22
  as Phase 1; FR-003, FR-005, FR-007 closed 2026-07-23 as Phase 3; FR-010, FR-011 closed
  2026-07-23 as Phase 4).
- **Remaining gaps**: none.
- **Phase 1 outcome**: 1 real bug found and fixed (FR-009 `ResetPassword` user-enumeration —
  see Notes above), 1 accidental-pass test replaced with 3 real subtests (FR-001), 1
  excluded-from-build file re-included with a stale-value fix and redundancy removed (FR-004),
  and 3 genuine zero-coverage gaps closed with no bug found (FR-002, FR-008, FR-012). Full
  before/after detail in `sbr/remediation-plan.md`.
- **Phase 2 outcome**: FR-006's 4 abuse-path reject clauses (already-registered email,
  expired/used/invalid invitation) and the join-request-linked-as-`JOINED` clause unit-tested.
  No bug found.
- **Phase 3 outcome**: FR-003's 2FA code-reuse-after-consumption rejection, FR-005's `Logout`
  FCM-obsolete-marking, and FR-007's org-existence check + persists-on-notify-failure clause all
  unit-tested. No bugs found in any of the three.
- **Phase 4 outcome**: FR-010's `RecordLegalConsent` (repository forwarding at L1;
  `INVALID_ARGUMENT` validation and real-DB idempotency at e2e) and FR-011's
  `GetUserConsentStatus` (pending-docs computation at L1; validation at e2e) both closed. No
  bugs found in either.
- **Phase 4 hardening pass (2026-07-23)**: a user follow-up flagged that several `Complete` rows
  carried unexplained blank tier cells, contrary to `sbr/README.md`'s own rule that every
  omitted tier must be justified in Notes. Re-audited every cell in this file: closed genuine
  L1 gaps for FR-001 (`Login`) and FR-004 (`RefreshToken`) — both previously evidenced only at
  L2/L3, constrained by the shared per-IP rate limiter — with mocked/DB-free unit tests that
  don't compete for that budget; closed genuine L2 gaps for FR-005 (`Logout`), FR-008
  (`ChangePassword`), and FR-009 (`ResetPassword`) with new real-DB integration tests
  (`tests/integration/auth_test.go`, new file); and added explicit Notes justification for every
  remaining blank that is a legitimate omission rather than a gap — FR-004/FR-011/FR-012 have no
  repository dependency to exercise at L2 ("N/A"), and FR-006/FR-007/FR-010 already have their
  real-DB effects asserted directly at L3, making a separate L2 test redundant
  ("intentionally not separately tested"). No bugs found in any of the 5 newly-added tests.
- **Unclassified**: none — every row above was resolved to either cited evidence (including
  citing why it's insufficient) or an explicit, named gap.
