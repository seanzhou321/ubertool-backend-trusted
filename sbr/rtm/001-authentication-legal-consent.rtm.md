# Requirements Traceability Matrix — Authentication & Legal Consent

- **Source spec**: `specs/001-authentication-legal-consent/spec.md`
- **Tier mapping in effect**: `sbr/README.md` → "This repo's adapter" (L1=`tests/unit`,
  L2=`tests/integration`, L3=`tests/e2e`, Grounding=`tests/smoke`+`tests/e2e` journeys)
- **Generated**: 2026-07-22 by `speckit-sbr-audit`
- **Mode**: retrofit audit (as-built) — see project constitution Principle I

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding (Smoke) | Boundary Status | Notes |
|---|---|---|---|---|---|---|---|
| FR-001 | `Login` MUST authenticate against canonical `password_hash`, fall back to `pending_credentials`, and reject with a generic error if neither matches. | — | `Test2FAFlow > "Step1_Login_Should_Return_2FA_Token"` (`tests/integration/2fa_test.go:67`) — canonical-password success path only | `TestAuthService_E2E > "Login Flow - Canonical Password Success"` (`tests/e2e/auth_test.go:151`), `> "Login Flow - Pending Credential Fallback"` (`:230`), `> "Login Flow - Invalid Credentials Rejected"` (`:262`) | `TestDatabaseConnectivity > "UsersTableReachable_via_Login"` (`tests/smoke/smoke_test.go:142`) — liveness probe only | **Complete** | **Fixed 2026-07-22.** The e2e "Login Flow" subtest previously set `password_hash` to a non-bcrypt literal, making it an accidental-pass (see git history, this file's prior revision). Replaced with 3 real subtests using genuine bcrypt hashes: canonical-password success, `pending_credentials` fallback success, and reject-with-generic-error on a match to neither — all 3 clauses of this FR now directly verified end-to-end. |
| FR-002 | `Login` MUST issue a `2fa_pending` JWT and generate a 2FA code, using the random-code+email path when `two_fa.enabled=true` and the fixed-passcode/no-email path only when `false`. | `TestAuthService_Login_TwoFA_ProductionPath` (`tests/unit/auth_test.go:136`) | `Test2FAFlow > "Step1_Login_Should_Return_2FA_Token"` (`tests/integration/2fa_test.go:67`), `> "Step4_Verify2FA_With_Valid_Code_Should_Succeed"` (`:148`) — `two_fa.enabled=false` path | — | — | **Complete** | Fixed 2026-07-22: added a unit test constructing `authService` with `TwoFAConfig{Enabled: true}` directly, capturing the randomly-generated 5-digit code from the mocked email send, and confirming `Verify2FA` accepts that code (not the fixed test passcode). The `enabled=false` path was already covered at L2. |
| FR-003 | `Verify2FA` MUST require a valid `2fa_pending` token, reject an incorrect/consumed code, and on success issue access+refresh tokens and delete the code. | `TestAuthService_Verify2FA_CodeReuse` (`tests/unit/auth_test.go`) | `Test2FAFlow > "Step2_Verify2FA_With_Invalid_Code_Should_Fail"` (`tests/integration/2fa_test.go:95`), `> "Step3_Verify2FA_Without_Token_Should_Fail"` (`:126`), `> "Step4_Verify2FA_With_Valid_Code_Should_Succeed"` (`:148`) | — (active build) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** Confirms a valid code succeeds once, and a second Verify2FA call with the same (now-consumed) code fails with `ErrInvalid2FACode`. No bug found. |
| FR-004 | `RefreshToken` MUST require a valid, non-expired `refresh`-type token and issue a new access/refresh pair with the same identity claims. | — | — | `TestAuthService_E2E > "Login Flow - Canonical Password Success"` (`tests/e2e/auth_test.go:151`) | — | **Complete** | **Fixed 2026-07-22.** `jwt_verification_test.go_` (excluded from the build, Known Discrepancy 6) was renamed into the build; its 2FA code was stale (`"123456"` vs. the config's `"47291"`) and would have failed immediately once compiled. Its 2 subtests were also fully redundant with each other. Consolidated into the "Canonical Password Success" subtest above (folding in JWT claims decoding + refresh assertions) to stay within the shared per-IP Login rate limiter's 3-attempt burst (FR-012) across this file's other 2 Login-calling subtests — see `sbr/remediation-plan.md`. |
| FR-005 | `Logout` MUST mark the caller's FCM token(s) `OBSOLETE`; MUST NOT be relied on to invalidate JWTs. | `TestAuthService_Logout` (`tests/unit/auth_test.go`) — 2 subtests: marks tokens obsolete, a MarkObsoleteByDevice failure does not fail logout | — | — | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** No bug found. |
| FR-006 | `UserSignup` MUST require a valid/unused/unexpired invitation matching the email, reject if the user already exists, and on success create the user, mark the invitation used, link any originating join request `JOINED`, and add the user as `MEMBER`. | `TestAuthService_Signup` (`tests/unit/auth_test.go`) — 5 subtests: already-registered reject, expired-invitation reject, used-invitation reject, invalid-invitation-code reject, success-links-originating-join-request-as-JOINED | — | `TestAuthService_E2E > "Signup with Valid Invitation"` (`tests/e2e/auth_test.go:25`) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** All 4 abuse-path reject clauses and the join-request-linked-as-`JOINED` clause now unit-tested. No bug found — production behavior matched the spec exactly. |
| FR-007 | `RequestToJoinOrganization` MUST create the `join_requests` row before notifying the admin, verify the org exists, and verify `admin_email` holds `ADMIN`/`SUPER_ADMIN` before notifying. | `TestAuthService_RequestToJoin` (`tests/unit/auth_test.go`) — 3 subtests: success, rejects a non-existent org, persists the join request even when admin verification subsequently fails | — | `TestAuthService_E2E > "RequestToJoin Organization"` (`tests/e2e/auth_test.go:70`) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** The FR's most distinctive clause — the `join_requests` row persisting even when admin verification subsequently fails (spec.md Edge Cases / US3 Scenario 6) — is now regression-locked. No bug found. |
| FR-008 | `ChangePassword` MUST verify the old password (canonical or valid temporary credential) and on success update the canonical hash and stamp any outstanding temporary credential used. | `TestAuthService_ChangePassword` (`tests/unit/auth_test.go:208`) — 3 subtests: canonical-password success, pending-credential-fallback success, wrong-old-password reject | — | — | — | **Complete** | Fixed 2026-07-22. No bug found — production behavior matched the spec exactly; this was purely a coverage gap. |
| FR-009 | `ResetPassword` MUST return an identical generic success response regardless of account existence, and only create/email a temporary credential when the account exists. | `TestAuthService_ResetPassword` (`tests/unit/auth_test.go:266`) — 2 subtests | — | — | — | **Complete** | **Fixed 2026-07-22 — real bug found and fixed.** `authService.ResetPassword`'s not-found branch returned a non-nil `error`, which the gRPC handler propagated as a distinguishable error response instead of the same generic success the found-user path returns — a user-enumeration vulnerability defeating this FR's entire purpose. Fixed in `internal/service/auth.go` (not-found branch now returns `nil`); regression-locked by `TestAuthService_ResetPassword/Non-existent_email_returns_no_error`. |
| FR-010 | `RecordLegalConsent` MUST reject empty `doc_names`/`version` with `INVALID_ARGUMENT` and MUST be idempotent under repeat calls. | — | — | — | — | **Gap — all tiers** | `MockLegalConsentRepo` exists (`tests/unit/mocks_test.go:561`) and is wired into two `AuthService` constructors in `tests/unit/auth_test.go`, but only for dependency injection into unrelated `ValidateInvite`/`RequestToJoin` tests — no test ever sets an expectation on it or calls `RecordLegalConsent`. |
| FR-011 | `GetUserConsentStatus` MUST reject empty `current_version` with `INVALID_ARGUMENT` and MUST compute `pending_docs` against the fixed `domain.KnownLegalDocs` list. | — | — | — | — | **Gap — all tiers** | No test anywhere calls `GetUserConsentStatus`. |
| FR-012 | `Login` and `Verify2FA` MUST be rate-limited per client IP (3-attempt burst, 1 refill/3 min). | — | — | `TestAuthService_RateLimit_E2E` (`tests/e2e/rate_limit_test.go:30`) — isolated via the `ratelimit` build tag, run via `make test-e2e-rate-limit` | — | **Complete** | Fixed 2026-07-22. Confirmed real and correctly implemented (`internal/security/rate_limiter.go`): both Login and Verify2FA independently throttle after exactly 3 attempts. Isolated behind a build tag because it deliberately exhausts the shared, process-lifetime, per-IP limiter that this file's other Login/Verify2FA-calling tests also depend on — see `sbr/remediation-plan.md` for why. |

## Summary

- **12 FR-IDs audited, 10 fully `Complete`** (FR-001, FR-002, FR-004, FR-006, FR-008, FR-009,
  FR-012 — FR-006 closed 2026-07-23 as SBR remediation Phase 2, rest closed 2026-07-22 as Phase
  1; FR-003, FR-005, FR-007 closed 2026-07-23 as Phase 3).
- **Remaining gaps**: FR-010 (`RecordLegalConsent`), FR-011 (`GetUserConsentStatus`) — tracked
  in `sbr/remediation-plan.md` Phase 4.
- **Phase 1 outcome**: 1 real bug found and fixed (FR-009 `ResetPassword` user-enumeration —
  see Notes above), 1 accidental-pass test replaced with 3 real subtests (FR-001), 1
  excluded-from-build file re-included with a stale-value fix and redundancy removed (FR-004),
  and 3 genuine zero-coverage gaps closed with no bug found (FR-002, FR-008, FR-012). Full
  before/after detail in `sbr/remediation-plan.md`.
- **Phase 2 outcome**: FR-006's 4 abuse-path reject clauses (already-registered email,
  expired/used/invalid invitation) and the join-request-linked-as-`JOINED` clause unit-tested.
  No bug found.
- **Phase 3 outcome**: FR-003's 2FA code-reuse-after-consumption rejection, FR-005's `Logout`
  FCM-obsolete-marking, and FR-007's org-existence check + persists-on-notify-failure clause all
  unit-tested. No bugs found in any of the three.
- **Unclassified**: none — every row above was resolved to either cited evidence (including
  citing why it's insufficient) or an explicit, named gap.
