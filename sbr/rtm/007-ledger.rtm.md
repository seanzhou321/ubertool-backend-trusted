# SBR Requirements Traceability Matrix: 007-ledger

**Source Spec**: `specs/007-ledger/spec.md`
**Adapter**: `sbr/README.md` (This repo's adapter)
**Generated**: 2026-07-29
**Mode**: retrofit audit

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding | Boundary Status | Planned Tests | Notes |
|---|---|---|---|---|---|---|---|---|
| FR-001 | `GetBalance` MUST return the caller's `balance_cents` for the given org, along with `last_updated_on` populated from `users_orgs.last_balance_updated_on` (empty string if `NULL`). | `TestLedgerService_GetBalance` > "Success" (tests/unit/ledger_service_test.go:13,18); `TestLedgerService_GetBalance_ReturnsLastUpdatedOn` > "returns the date the repository provides" / "empty string when the repository has never recorded an update" (tests/unit/ledger_service_test.go:32,37,45) | `TestLedgerRepository_GetBalance` > "Returns the caller's balance_cents for the given org" (tests/integration/ledger_test.go:101,119), sibling "Errors for a user with no membership in the org" (125); `TestLedgerRepository_GetBalance_IncludesLastUpdatedOn` > "returns the seeded date" / "empty string when last_balance_updated_on is NULL" (tests/integration/ledger_test.go:135,157,163) — real DB, both the populated and NULL cases | `TestLedgerService_E2E` > "GetBalance" (tests/e2e/ledger_test.go:14,24); `TestLedgerService_E2E` > "GetBalance_LastUpdatedOn" (tests/e2e/ledger_test.go:43) — real gRPC call, seeds `last_balance_updated_on` via direct UPDATE, asserts `resp.LastUpdatedOn` | — | Complete | — | Both the core balance-return behavior and the `last_updated_on` sub-claim (previously the sole tracked gap on this FR) are now verified at all three tiers, including the NULL/never-updated edge case at L1 and L2. Sqlmock-level repo test also updated (`tests/unit/repos/ledger_test.go:42,52,63`) to assert the new two-column SELECT and Scan. |
| FR-002 | `GetTransactions` MUST return only the caller's own transactions, most recent first, with an accurate `total_count` independent of the current page. | `TestLedgerService_GetTransactions` > "Success" (tests/unit/ledger_service_test.go:54,59) — mocked passthrough only | `TestLedgerRepository_GetTransactions_IsolationOrderingPagination` (tests/integration/ledger_test.go:23) — subtests "Isolation: excludes another user's transactions in the same org" (60), "Ordering: most recent first" (70), "Pagination: total_count stays accurate across pages, page 2 returns the remainder" (79) | `TestLedgerService_E2E` > "GetTransactions" (tests/e2e/ledger_test.go:62) — weaker: only `GreaterOrEqual` checks, no ordering/isolation/pagination assertion | — | Complete | — | Unchanged from prior audit — best-evidenced FR in this file. Not touched by this pass. |
| FR-003 | `GetLedgerSummary` MUST return the caller's balance and a per-status rental count (as renter OR owner) for a single given org, limited to rentals created within `number_of_months` months when greater than zero (unbounded when omitted), plus `recent_transactions` populated with the caller's 5 most-recent `ledger_transactions` rows in that org, most recent first, unaffected by `number_of_months`. | `TestLedgerService_GetLedgerSummary` > "Success" (tests/unit/ledger_service_test.go:75,80); `TestLedgerService_GetLedgerSummary_FiltersByMonths` > "single-org: numberOfMonths forwarded to GetSummary" (117,118); `TestLedgerService_GetLedgerSummary_IncludesRecentTransactions` > "Success" (tests/unit/ledger_service_test.go:95,100) — proves the service passes `RecentTransactions` through unchanged | `TestLedgerRepository_GetSummary` (tests/integration/ledger_test.go:175) — seeds renter-role AND owner-role rentals, proves the renter+owner union; `TestLedgerRepository_GetSummary_FiltersByMonths` (227) — proves `number_of_months=0` vs. `=3` date-window behavior, balance unaffected; `TestLedgerRepository_GetSummary_IncludesRecentTransactions` (tests/integration/ledger_test.go:277) — seeds 7 transactions, asserts exactly the 5 most recent are returned newest-first, and that another user's transaction in the same org never leaks in | `TestLedgerService_E2E` > "GetLedgerSummary" (tests/e2e/ledger_test.go:92,130) and "GetLedgerSummary applies number_of_months as a real filter" (155); `TestLedgerService_E2E` > "GetLedgerSummary_RecentTransactions" (tests/e2e/ledger_test.go:130) — real gRPC call, seeds 7 transactions, asserts `len(resp.RecentTransactions) == 5` and newest-first ordering | — | Complete | — | The single-org balance+per-status-count behavior, the `number_of_months` filter, and now `recent_transactions` (capped at 5, most-recent-first, isolated from other users) are all genuinely verified at all three tiers. Cross-org rollup is FR-004's requirement, not FR-003's. |
| FR-004 *(multi-org, PRD 3.1)* | `GetLedgerSummary` MUST roll up balance + per-status rental counts across ALL orgs the caller belongs to when `organization_id` is omitted (0), with the same `number_of_months` filtering applied per-org before aggregation, and `recent_transactions` populated with the caller's 5 most-recent transactions across ALL their orgs, most recent first. | `TestLedgerService_GetLedgerSummary_RollsUpAcrossOrgs` > "organization_id omitted calls the cross-org rollup, not the single-org lookup" (tests/unit/ledger_service_test.go:151,156); `TestLedgerService_GetLedgerSummary_FiltersByMonths` > "cross-org rollup: numberOfMonths forwarded to GetSummaryAllOrgs" (131) | `TestLedgerRepository_GetSummary_CrossOrgRollup` (tests/integration/ledger_test.go:331) — two orgs, both roles, plus a third org/user that must not leak in; `TestLedgerRepository_GetSummaryAllOrgs_FiltersByMonths` (448) — two orgs, one recent + one 10-month-old rental in different orgs; `TestLedgerRepository_GetSummaryAllOrgs_IncludesRecentTransactions` (tests/integration/ledger_test.go:387) — seeds transactions in two different orgs for the same user, proves the 5-most-recent cap spans across orgs (not per-org), newest-first, isolated from another user's transaction | `TestLedgerService_E2E` > "GetLedgerSummary rolls up across all orgs when organization_id is omitted" (tests/e2e/ledger_test.go:187) | — | Complete (a, b, c, d) | — | (a)/(b)/(c) unchanged from prior audit. (d): `getRecentTransactions` (internal/repository/postgres/ledger.go:145) is a shared helper parameterized by `WHERE` clause — `GetSummary` calls it scoped to `user_id AND org_id`, `GetSummaryAllOrgs` calls it scoped to `user_id` only, so the cross-org cap-of-5 is proven by a real-DB test seeding transactions in two distinct orgs and asserting the newest 5 win regardless of which org they're in. |

## Summary
- Total FRs: 4
- Complete: 4 (FR-001, FR-002, FR-003, FR-004 — all fully verified, no self-documented unbuilt scope remaining)
- Gaps: 0
- Unclassified: 0

**Resolved since last audit**: Known Discrepancy 1 (`GetBalance.last_updated_on`) and Known Discrepancy 3 (`GetLedgerSummary.recent_transactions`) — both previously tracked as intentionally-unbuilt, self-documented scope, now implemented and verified at all three tiers for every applicable path (single-org and cross-org). `GetLedgerSummaryResponse` gained a new proto field (`recent_transactions`, field 3) requiring `make proto-gen` regeneration; this is additive (new field number), not wire-breaking for existing clients that don't read it.

This repo has no ledger-table smoke/grounding coverage (`tests/smoke/smoke_test.go` covers only Orgs/Users/Invitations) — unchanged from prior audit, not in scope for this pass.
