# SBR Requirements Traceability Matrix: 007-ledger

**Source Spec**: `specs/007-ledger/spec.md`
**Adapter**: `sbr/README.md` (This repo's adapter)
**Generated**: 2026-07-29 (full re-audit — see "2026-07-29 re-audit note" below)
**Mode**: retrofit audit

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding | Boundary Status | Planned Tests | Notes |
|---|---|---|---|---|---|---|---|---|
| FR-001 | `GetBalance` MUST return the caller's `balance_cents` for the given org. MUST NOT be assumed to also return a populated `last_updated_on` (Known Discrepancy 1). | `TestLedgerService_GetBalance` > "Success" (tests/unit/ledger_service_test.go:12,17) — mocked passthrough | `TestLedgerRepository_GetBalance` > "Returns the caller's balance_cents for the given org" (tests/integration/ledger_test.go:101,119), sibling "Errors for a user with no membership in the org" (125) — real DB | `TestLedgerService_E2E` > "GetBalance" (tests/e2e/ledger_test.go:13,23) | — | Complete (core); expected gap (sub-claim) | N/A for the `last_updated_on` sub-claim — the field is not populated by the service at all (documented as intentionally unbuilt); a test would have nothing to assert until that's implemented. Route to a feature task if/when `last_updated_on` support is added. | Core balance-return behavior genuinely verified at all three tiers. The `last_updated_on` non-population sub-claim has no test anywhere (grepped `tests/` for `LastUpdatedOn`/`last_updated_on` — zero hits) — correctly untested, since spec.md says not to assume it's built. No ledger-table smoke test exists; the e2e journey is the closest grounding analog. |
| FR-002 | `GetTransactions` MUST return only the caller's own transactions, most recent first, with an accurate `total_count` independent of the current page. | `TestLedgerService_GetTransactions` > "Success" (tests/unit/ledger_service_test.go:26,31) — mocked passthrough only | `TestLedgerRepository_GetTransactions_IsolationOrderingPagination` (tests/integration/ledger_test.go:23) — subtests "Isolation: excludes another user's transactions in the same org" (60), "Ordering: most recent first" (70), "Pagination: total_count stays accurate across pages, page 2 returns the remainder" (79) | `TestLedgerService_E2E` > "GetTransactions" (tests/e2e/ledger_test.go:42) — weaker: only `GreaterOrEqual` checks, no ordering/isolation/pagination assertion | — | Complete | — | Best-evidenced FR in this file — the L2 test is a purpose-built regression test naming exactly this FR's isolation/ordering/pagination claims and proving each independently against a real DB. |
| FR-003 | `GetLedgerSummary` MUST return the caller's balance and a per-status rental count (as renter OR owner) for a single given org. MUST NOT be assumed to roll up across orgs, apply `number_of_months` filtering, or include recent transactions (Known Discrepancies 2-4 — none implemented). | `TestLedgerService_GetLedgerSummary` > "Success" (tests/unit/ledger_service_test.go:47,52) — mocked passthrough | `TestLedgerRepository_GetSummary` (tests/integration/ledger_test.go:136) — seeds renter-role AND owner-role rentals, explicitly proves the renter+owner union (`StatusCount["ACTIVE"]=2`) | `TestLedgerService_E2E` > "GetLedgerSummary" (tests/e2e/ledger_test.go:72) — passes `NumberOfMonths: 3` but does not seed old-vs-new rentals or assert filtering | — | Complete (core, single-org scope) | N/A for the three excluded sub-claims — none are implemented (confirmed by reading `internal/service/ledger.go`); a test would have nothing to assert. See FR-004 for the rollup piece specifically, which is tracked as its own full gap below. | The single-org balance+per-status-count behavior is genuinely verified at all three tiers. The three as-built exclusions (rollup, months-filtering, recent-transactions) are correctly untested — matches spec's explicit claim, and rollup itself is FR-004's job, not FR-003's. |
| FR-004 *(multi-org, PRD 3.1)* | `GetLedgerSummary` MUST roll up balance + per-status rental counts across ALL orgs the caller belongs to when `organization_id` is omitted (0). Currently a Known Discrepancy (Gap 2) — as-built returns a "no rows" error instead. | — | — | — | — | Gap — L1, L2, L3, Grounding | N/A — this is a missing-feature gap, not a missing-test gap. `internal/service/ledger.go` has no `orgID==0` branch anywhere in the `GetBalance`/`GetLedgerSummary` call chain, so there is no rollup behavior for a test to assert yet. Route to a feature/bugfix task (implement the rollup) before any test can be meaningfully written; once implemented, add L1 (`TestLedgerService_GetLedgerSummary_RollsUpAcrossOrgs`), L2 (`TestLedgerRepository_GetSummary_CrossOrgRollup`, real DB, multiple orgs), and L3 (`TestLedgerService_E2E` > "GetLedgerSummary rolls up across all orgs when organization_id is omitted") tests mirroring FR-003's pattern. In the meantime, a narrower regression test asserting the *current* "no rows" failure mode would at least document today's behavior — recommended as an interim step. | **True, complete verification gap.** `internal/service/ledger.go` confirms no `orgID==0` branch exists anywhere in the `GetBalance`/`GetLedgerSummary` call chain — `organization_id` is passed straight through. No test anywhere calls either RPC with `organization_id=0`, so there isn't even Gap-documenting evidence of the current "no rows" failure mode. Recommend as a candidate for `speckit-sbr-bugfix` or a dedicated follow-up task per spec's own SC-002. |

## Summary
- Total FRs: 4
- Complete: 3 (FR-001, FR-002, FR-003 — each with a self-documented, intentionally-unbuilt sub-scope excluded)
- Gaps: 1 (FR-004 — full gap, all tiers)
- Unclassified: 0

**Gaps**: FR-004 (cross-org rollup unimplemented and untested at every tier, including the weaker bar of documenting the current failure mode — this is a missing-feature gap, not just a missing-test gap; see Planned Tests column).

## 2026-07-29 re-audit note

This file replaces a 2026-07-27-generated version whose citations were **fabricated** — e.g. FR-001 (`GetBalance`) cited `TestAuthService_GetUserConsentStatus`, FR-003/FR-004 cited `TestAuthService_RequestToJoin`/`TestAdminService_RequiresAdminRole`/`TestAdminService_E2E > "RejectRequestToJoin..."` — real, already-existing, already-passing tests for Auth/Admin domains, not not-yet-written placeholders for a ledger test plan. The prior version also marked all 4 FRs `Complete` with "Gaps: 0," directly contradicting spec.md's own explicit admission that FR-004's cross-org rollup is a documented, unimplemented Known Discrepancy. Every citation here was independently re-verified against the actual test files and, for FR-004, against `internal/service/ledger.go` itself to confirm no `organization_id=0` handling exists. This version adds a **Planned Tests** column (see `sbr/README.md`'s "RTM schema" section); note that FR-004's entry there is explicitly routed to a feature/bugfix task rather than a test stub, since the underlying rollup behavior doesn't exist to test yet. This repo also has no ledger-table smoke/grounding coverage at all (`tests/smoke/smoke_test.go` covers only Orgs/Users/Invitations) — not previously disclosed in any prior RTM version.
