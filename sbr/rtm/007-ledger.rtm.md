# SBR Requirements Traceability Matrix: 007-ledger

**Source Spec**: `specs/007-ledger/spec.md`
**Adapter**: `sbr/README.md` (This repo's adapter)
**Generated**: 2026-07-29
**Mode**: retrofit audit

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding | Boundary Status | Planned Tests | Notes |
|---|---|---|---|---|---|---|---|---|
| FR-001 | `GetBalance` MUST return the caller's `balance_cents` for the given org. MUST NOT be assumed to also return a populated `last_updated_on` (Known Discrepancy 1). | `TestLedgerService_GetBalance` > "Success" (tests/unit/ledger_service_test.go:12,17) — mocked passthrough | `TestLedgerRepository_GetBalance` > "Returns the caller's balance_cents for the given org" (tests/integration/ledger_test.go:101,119), sibling "Errors for a user with no membership in the org" (125) — real DB | `TestLedgerService_E2E` > "GetBalance" (tests/e2e/ledger_test.go:13,23) | — | Complete (core); expected gap (sub-claim) | N/A for the `last_updated_on` sub-claim — the field is not populated by the service at all (documented as intentionally unbuilt); a test would have nothing to assert until that's implemented. Route to a feature task if/when `last_updated_on` support is added. | Core balance-return behavior genuinely verified at all three tiers. The `last_updated_on` non-population sub-claim has no test anywhere (grepped `tests/` for `LastUpdatedOn`/`last_updated_on` — zero hits) — correctly untested, since spec.md says not to assume it's built. No ledger-table smoke test exists; the e2e journey is the closest grounding analog. |
| FR-002 | `GetTransactions` MUST return only the caller's own transactions, most recent first, with an accurate `total_count` independent of the current page. | `TestLedgerService_GetTransactions` > "Success" (tests/unit/ledger_service_test.go:26,31) — mocked passthrough only | `TestLedgerRepository_GetTransactions_IsolationOrderingPagination` (tests/integration/ledger_test.go:23) — subtests "Isolation: excludes another user's transactions in the same org" (60), "Ordering: most recent first" (70), "Pagination: total_count stays accurate across pages, page 2 returns the remainder" (79) | `TestLedgerService_E2E` > "GetTransactions" (tests/e2e/ledger_test.go:42) — weaker: only `GreaterOrEqual` checks, no ordering/isolation/pagination assertion | — | Complete | — | Best-evidenced FR in this file — the L2 test is a purpose-built regression test naming exactly this FR's isolation/ordering/pagination claims and proving each independently against a real DB. |
| FR-003 | `GetLedgerSummary` MUST return the caller's balance and a per-status rental count (as renter OR owner) for a single given org. MUST NOT be assumed to apply `number_of_months` filtering or include recent transactions (Known Discrepancies 2-3). | `TestLedgerService_GetLedgerSummary` > "Success" (tests/unit/ledger_service_test.go:47,52) — mocked passthrough | `TestLedgerRepository_GetSummary` (tests/integration/ledger_test.go:136) — seeds renter-role AND owner-role rentals, explicitly proves the renter+owner union (`StatusCount["ACTIVE"]=2`) | `TestLedgerService_E2E` > "GetLedgerSummary" (tests/e2e/ledger_test.go:72) — passes `NumberOfMonths: 3` but does not seed old-vs-new rentals or assert filtering | — | Complete (core, single-org scope) | N/A for the two excluded sub-claims — neither is implemented; a test would have nothing to assert. | The single-org balance+per-status-count behavior is genuinely verified at all three tiers. The months-filtering and recent-transactions exclusions are correctly untested — matches spec's explicit claim. Cross-org rollup is FR-004's requirement, not FR-003's. |
| FR-004 *(multi-org, PRD 3.1)* | `GetLedgerSummary` MUST roll up balance + per-status rental counts across ALL orgs the caller belongs to when `organization_id` is omitted (0), with the same `number_of_months` filtering applied per-org before aggregation. | `TestLedgerService_GetLedgerSummary_RollsUpAcrossOrgs` > "organization_id omitted calls the cross-org rollup, not the single-org lookup" (tests/unit/ledger_service_test.go:64) | `TestLedgerRepository_GetSummary_CrossOrgRollup` (tests/integration/ledger_test.go:189) — two orgs, both roles, plus a third org/user that must not leak in | `TestLedgerService_E2E` > "GetLedgerSummary rolls up across all orgs when organization_id is omitted" (tests/e2e/ledger_test.go:110) | — | Complete (a, b — balance + rental-count rollup); Gap — (c) `number_of_months`, all tiers | (c) only: N/A — missing-feature gap, not missing-test. `number_of_months` is read by nothing in this domain for *any* path (Known Discrepancy 2), so there is no filtering behavior yet for a test to assert. Once fixed for the single-org path, extend `GetSummaryAllOrgs`'s per-org queries with the same date filter and add L1/L2/L3 tests mirroring FR-003's (currently nonexistent) months-filtering tests. | (a)/(b): `ledgerRepository.GetSummaryAllOrgs` sums `balance_cents` across every `users_orgs` row regardless of membership status, and aggregates rental counts across every org the user belongs to (no `org_id` filter — `rentals_shared_org_check` already guarantees every rental implies membership). (c): `number_of_months` isn't implemented for the single-org path either (Known Discrepancy 2), so it's tracked as its own gap rather than folded into a false `Complete`. |

## Summary
- Total FRs: 4
- Complete: 3 (FR-001, FR-002, FR-003 — each with a self-documented, intentionally-unbuilt sub-scope excluded)
- Gaps: 1 (FR-004 — partial: (a)/(b) complete, (c) `number_of_months` still a full gap)
- Unclassified: 0

**Gaps**: FR-004(c) (`number_of_months` per-org filtering — unimplemented for every path, single-org or rolled-up; see Known Discrepancy 2 and Planned Tests column).

This repo has no ledger-table smoke/grounding coverage (`tests/smoke/smoke_test.go` covers only Orgs/Users/Invitations).
