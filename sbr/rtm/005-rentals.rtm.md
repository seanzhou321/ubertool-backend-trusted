# Requirements Traceability Matrix — Rentals

- **Source spec**: `specs/005-rentals/spec.md`
- **Tier mapping in effect**: `sbr/README.md` → "This repo's adapter" (L1=`tests/unit`,
  L2=`tests/integration`, L3=`tests/e2e`, Grounding=`tests/smoke`+`tests/e2e` journeys)
- **Generated**: 2026-07-22 by `speckit-sbr-audit`
- **Mode**: retrofit audit (as-built) — see project constitution Principle I

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding (Smoke) | Boundary Status | Notes |
|---|---|---|---|---|---|---|---|
| FR-001 | `CreateRentalRequest` MUST validate `end_date > start_date`, MUST snapshot the tool's price fields onto the rental, MUST compute `total_cost_cents` from that snapshot. (Overlap rejection explicitly NOT required — Known Discrepancy 1.) | `TestRentalService_CreateRentalRequest > "Success"` (`tests/unit/rental_test.go:45`) — happy path only | — | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:23`, via `doCreateRentalRequest`, reused by 3 more subtests) and `TestLedgerService_E2E > "Ledger Updates After Rental Completion"` (`tests/e2e/ledger_test.go:131`) — happy path only | — | **Gap — L1 (partial), L2, L3 (partial)** | The snapshot→cost-computation clause is reasonably covered (unit test asserts `total_cost_cents=2000` for a 2-day rental against a 1000-cents/day snapshot). But the `end_date <= start_date` rejection clause (Acceptance Scenario 2, error message "end date must be after start date...") has **zero test evidence at any tier** — `grep -r "end date must be after start date"` under `tests/` returns nothing. No integration-tier test invokes `CreateRentalRequest` through the service at all. |
| FR-002 | `ApproveRentalRequest` MUST require the caller to be the tool's owner and the rental to be `PENDING`. | — | — | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:31`) + 2 more subtests + `TestLedgerService_E2E > "Ledger Updates After Rental Completion"` (`tests/e2e/ledger_test.go:150`) — happy path only | — | **Gap — L1, L2, L3 (partial)** | `TestRentalHandler_ApproveRentalRequest > "Success"` (`tests/unit/handlers/rental_handler_test.go:58`) exists but mocks `RentalService.ApproveRentalRequest` itself (`rentalSvc.On("ApproveRentalRequest",...).Return(rental, nil)`) — it verifies gRPC handler marshaling, not the service's owner/`PENDING` gate, so it is **not** counted as L1 evidence for this FR. Every e2e occurrence calls `ApproveRentalRequest` as the owner on a freshly-created `PENDING` rental; no test anywhere exercises a non-owner caller or a non-`PENDING` rental being rejected. |
| FR-003 | `FinalizeRentalRequest` MUST require the caller to be the renter and the rental to be `APPROVED`, and MUST set the tool's status to `RENTED` on success. | `TestRentalService_FinalizeRentalRequest > "Success"` (`tests/unit/rental_test.go:252`) — happy path only, but does assert the tool-status-`RENTED` update via `mock.MatchedBy` (`:263-265`) | — | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:34`, tool-status assertion at `:37`) + 2 more subtests + `TestLedgerService_E2E` (`tests/e2e/ledger_test.go:160`) — happy path only | — | **Gap — L2; L1 & L3 (partial)** | The `RENTED`-on-success clause is well covered at L1 and L3. No test at any tier exercises the two MUST-reject clauses (non-renter caller, non-`APPROVED` rental) despite Acceptance Scenario 6 documenting the latter explicitly. |
| FR-004 | `CompleteRental` MUST require a participant caller and `ACTIVE`/`SCHEDULED`/`OVERDUE` status, MUST recompute cost from the rental's own price snapshot (never the tool's current prices), and MUST only create ledger transactions / update balances when `charge_billsplit=true`. | `TestRentalService_CompleteRental` (`tests/unit/rental_test.go:78`) — subtests `"Success with charge_billsplit=true"` (`:111`), `"Success with charge_billsplit=false"` (`:144`), `"Settlement notification reminder text when charge_billsplit=false"` (`:176`) | `TestRentalService_CompleteRental_Integration` (`tests/integration/rental_ledger_test.go:184`) — 5 subtests, calls `service.CompleteRental` directly against a real DB | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:39`, charge_billsplit=true) and `"Full Rental Lifecycle with charge_billsplit=false"` (`:130`); also `TestLedgerService_E2E > "Ledger Updates After Rental Completion"` (`tests/e2e/ledger_test.go:184`) — happy path only | — | **Complete** | **Fixed 2026-07-22.** The prior "integration" test (`TestRentalAndLedger_Integration > "Full Lifecycle"`) bypassed `service.CompleteRental` entirely via direct repo writes, contradicting spec.md's "Current Test Coverage Baseline" claim of "(unit + integration)" coverage. Replaced with a new test that calls the real service method and covers: snapshot-vs-tool-price recomputation (tool's live price deliberately set to a different value), `charge_billsplit=true` balance/ledger effects, `charge_billsplit=false` no-op, non-participant reject, non-completable-status reject. No bug found — production behavior matched the spec exactly. spec.md's Coverage Baseline corrected. |
| FR-005 | Every return-date-change transition (`ChangeRentalDates` active-rental case, `ApproveReturnDateChange`, `RejectReturnDateChange`, `AcknowledgeReturnDateRejection`, `CancelReturnDateChange`) MUST recompute `total_cost_cents` from the price snapshot whenever `end_date` changes. | `ChangeRentalDates`: `TestRentalService_ChangeRentalDates > "Renter Extension Active"` (`tests/unit/rental_test.go:373`), `> "Renter Updates Pending Extension Request"` (`:397`). `RejectReturnDateChange`: `TestRentalService_RejectReturnDateChange` (`:447`), 8 subtests incl. `"Success - Owner sets counter-proposal"` (`:470`, asserts recomputed cost). `ApproveReturnDateChange`, `AcknowledgeReturnDateRejection`, `CancelReturnDateChange`: **no unit test** | `ChangeRentalDates` + `ApproveReturnDateChange`: `TestRentalDateChange_Integration > "Activate -> Extend -> Approve"` (`tests/integration/rental_ledger_test.go:230`, calls at `:273` and `:287`). `RejectReturnDateChange`, `AcknowledgeReturnDateRejection`, `CancelReturnDateChange`: **none** | `ChangeRentalDates` + `ApproveReturnDateChange`: `TestRentalService_E2E > "Update Extension Request While Pending (RETURN_DATE_CHANGED)"` (`tests/e2e/rental_test.go:65`, two successive `ChangeRentalDates` calls with cost-recompute assertions via `assertExtensionDatesInDB`, then `ApproveReturnDateChange` at `:89`). `RejectReturnDateChange`, `AcknowledgeReturnDateRejection`, `CancelReturnDateChange`: **none** | — | **Gap — L1 (2/5 RPCs), L2 (3/5 RPCs), L3 (3/5 RPCs)** | This FR bundles 5 RPCs with uneven coverage: `ChangeRentalDates` is well covered at all three tiers with explicit cost-recompute assertions. `ApproveReturnDateChange` has L2+L3 evidence but no dedicated unit test. `RejectReturnDateChange` has thorough L1 coverage (including its own recompute assertion) but is **never called from any L2 or L3 test**. `AcknowledgeReturnDateRejection` and `CancelReturnDateChange` have **zero test evidence at any tier** — the only occurrences of either name in `tests/` are `MockRentalService` interface-stub implementations (`tests/unit/handlers/mocks_test.go:133`, `:140`), never invoked or asserted against. This matches spec.md's own "Current Test Coverage Baseline" section, which already lists both as not covered. |
| FR-006 | `GetRental` MUST grant access to the rental's renter and owner. (As-built, admin access is explicitly NOT required — Known Discrepancy 4.) | — | — | — | — | **Gap — all tiers** | The only occurrence of `GetRental` in `tests/` is the `MockRentalService.GetRental` interface-stub implementation (`tests/unit/handlers/mocks_test.go:93`) — it is never invoked or asserted against by any test. Zero evidence at any tier for either the renter/owner-grant clause or the (explicitly out-of-scope) admin-reject behavior. This matches spec.md's own Known Discrepancy 4 / "Current Test Coverage Baseline" section — this RTM confirms, not newly discovers, that gap. |

## Summary

- **6 FR-IDs audited, 1 fully `Complete`** (FR-004, closed 2026-07-22 as SBR remediation Phase 1).
- **Remaining gaps**: FR-001 (date-order rejection), FR-002 (owner/PENDING reject — Phase 2),
  FR-003 (renter/APPROVED reject), FR-005 (2 of 5 sub-RPCs), FR-006 (`GetRental`) — tracked in
  `sbr/remediation-plan.md`.
- **Phase 1 outcome**: FR-004's spec-vs-test discrepancy resolved — the fake integration test
  (direct repo writes bypassing `service.CompleteRental`) replaced with a real one exercising
  snapshot-recomputation, the `charge_billsplit` gate, and the participant/status reject
  clauses. No bug found. spec.md's Coverage Baseline corrected.
- **Unclassified**: none — every row above was resolved to either cited evidence or an
  explicit, named gap.
