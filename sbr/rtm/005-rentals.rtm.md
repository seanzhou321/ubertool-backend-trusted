# SBR Requirements Traceability Matrix: 005-rentals

**Source Spec**: `specs/005-rentals/spec.md`
**Adapter**: `sbr/README.md` (This repo's adapter)
**Generated**: 2026-07-29
**Updated**: 2026-07-29 — KD-1/2/3/4 fixed (FR-001, FR-006, FR-009, FR-010)
**Mode**: retrofit audit

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding | Boundary Status | Planned Tests | Notes |
|---|---|---|---|---|---|---|---|---|
| FR-001 | `CreateRentalRequest` MUST validate `end_date > start_date`, snapshot the tool's price fields, compute `total_cost_cents` from that snapshot, and MUST reject an overlapping-date request for the same tool (Known Discrepancy 1, RESOLVED). | `TestRentalService_CreateRentalRequest` (tests/unit/rental_test.go:18) — "Success" (asserts snapshot/cost), "Rejects an end date that is not after the start date", "Rejects an overlapping date range for the same tool (KD-1)" | — | `TestRentalService_E2E` > "Full Rental Lifecycle" (tests/e2e/rental_test.go:23) — 2-day rental, asserts `total_cost_cents` computed as 2×daily price | Same e2e journey (full-journey grounding per the adapter's Grounding tier definition) | Complete (L1, L3); Gap — L2 | L2: exercise `HasOverlappingRental` (`internal/repository/postgres/rental.go`) against a real DB with two overlapping rentals inserted directly, confirming the `EXISTS` query's date-range semantics against real Postgres date arithmetic. | No L2: `TestRentalAndLedger_Integration` (tests/integration/rental_ledger_test.go:98) inserts a `Rental` row directly via the repository, bypassing `CreateRentalRequest` entirely. L1+L3 judged sufficient for the validation/snapshot behavior; the new overlap-check repository method has L1-equivalent mock coverage only (`HasOverlappingRental` mocked in `TestRentalService_CreateRentalRequest`), not yet exercised against real Postgres. |
| FR-002 | `ApproveRentalRequest` MUST require the caller to be the tool's owner and the rental to be `PENDING`. | `TestRentalService_ApproveRentalRequest` (tests/unit/rental_test.go:399) — "Success as the tool's owner on a PENDING rental", "Rejects a caller who is not the tool's owner", "Rejects a rental that is not PENDING" | — | `TestRentalService_E2E` > "Full Rental Lifecycle" (tests/e2e/rental_test.go:23, step `doApproveRentalRequest`) | Same e2e journey | Complete | — | No dedicated L2. |
| FR-003 | `FinalizeRentalRequest` MUST require the caller to be the renter and the rental to be `APPROVED`, and MUST set the tool's status to `RENTED`. | `TestRentalService_FinalizeRentalRequest` (tests/unit/rental_test.go:458) — "Success", "Rejects a caller who is not the renter", "Rejects a rental that is not APPROVED" | — | `TestRentalService_E2E` > "Full Rental Lifecycle" (tests/e2e/rental_test.go:23, step `doFinalizeRentalRequest`, asserts `assertToolStatus(..., "RENTED")`) | Same e2e journey | Complete | — | Unit tests confirm the caller/status checks; the e2e journey confirms the tool-status side effect. No dedicated L2. |
| FR-004 | `CompleteRental` MUST require the caller to be the tool's owner (not renter), rental `ACTIVE`/`SCHEDULED`/`OVERDUE`, MUST recompute cost from the rental's own price snapshot, and MUST only create ledger transactions when `charge_billsplit = true`. | `TestRentalService_CompleteRental` (tests/unit/rental_test.go:219) — "Success with charge_billsplit=true", "Success with charge_billsplit=false", "Rejects a caller who is the renter, not the owner (SEC-RENTAL-005)" | `TestRentalService_CompleteRental_Integration` (tests/integration/rental_ledger_test.go:184) — purpose-built regression test for this exact FR, per its own doc comment | `TestRentalService_E2E` > "Full Rental Lifecycle" (charge_billsplit=true path, tests/e2e/rental_test.go:23) and "Full Rental Lifecycle with charge_billsplit=false" (203) | Same e2e journeys | Complete | — | Both `charge_billsplit` branches covered at all three tiers, including balance/ledger assertions at e2e. |
| FR-005 | Every return-date-change transition MUST recompute `total_cost_cents` from the rental's price snapshot whenever `end_date` changes. | `TestRentalService_ChangeRentalDates` (618), `TestRentalService_ApproveReturnDateChange` (1146), `TestRentalService_RejectReturnDateChange` (727), `TestRentalService_AcknowledgeReturnDateRejection` (1004), `TestRentalService_CancelReturnDateChange` (1074) — all in tests/unit/rental_test.go | `TestRentalDateChange_Integration` (tests/integration/rental_ledger_test.go:331) — exercises `service.NewRentalService` (not a repo bypass) against a real DB | `TestRentalService_E2E` > "Update Extension Request While Pending" (65), "Reject Extension Request with Counter-Proposal, Renter Acknowledges" (102), "Cancel Extension Request Before Owner Acts" (132) | Same e2e journeys | Complete | — | Thoroughly covered at all three tiers, unlike most other FRs in this file. |
| FR-006 | `GetRental` MUST grant access to the rental's renter, owner, and any ADMIN/SUPER_ADMIN of the rental's org (Known Discrepancy 4, RESOLVED). | `TestRentalService_GetRental` (tests/unit/rental_test.go:1219) — "Grants access to the renter", "Grants access to the owner", "Rejects a caller who is neither the renter nor the owner nor an org admin", "Rejects a caller who is not even a member of the rental's org", "Grants access to an admin/super-admin of the rental's org ... (KD-4)" | — | `TestRentalService_E2E` > "GetRental grants access to renter and owner, rejects others" (tests/e2e/rental_test.go:174) | Same e2e journey | Complete (L1, L3 for renter/owner/non-member); Gap — L3 for admin-grant | L3: extend "GetRental grants access to renter and owner, rejects others" (tests/e2e/rental_test.go) with an org-admin caller who is neither renter nor owner, asserting success — the admin-grant path is currently unit-tested only. | The e2e non-participant-rejection case predates KD-4's fix and doesn't yet cover the new admin-grant branch specifically. No L2. |
| FR-007 | `ListMyRentals`/`ListMyLendings` MUST OR-combine a `status` array (empty = all) and MUST treat `organization_id` as optional (0 = spans all the caller's orgs). | `TestRentalService_ListMyRentalsAndLendings` (tests/unit/rental_test.go:1278) — covers empty-status-forwarding for both RPCs; does **not** exercise `organization_id=0` (fixed at a non-zero org throughout) | `TestRentalRepository_ListByRenterAndOwner_OrgFilter` (tests/integration/rental_list_test.go:22) — "A specific organization_id filters to only that org's rentals", "organization_id=0 (omitted) returns the renter's rentals across all their orgs", and the `ListByOwner` equivalents | — | — | Gap — L3 | L3: `TestRentalService_E2E` > "ListMyRentals and ListMyLendings span all orgs when organization_id is omitted" (new, tests/e2e/rental_test.go) — create rentals/lendings in 2+ orgs for one caller, call both RPCs with `organization_id=0` via the real gRPC client, assert results span both orgs; add a status-filter subtest alongside it for full-stack OR-combine confirmation. | The `organization_id=0` (all-orgs) behavior is verified at L2 against a real DB, and OR-combine status logic at L1, but no e2e test calls `ListMyRentals`/`ListMyLendings` at all (`grep` for these RPC names in `tests/e2e/rental_test.go` returns nothing). |
| FR-008 *(multi-org, PRD 3.3, Organizations FR-012)* | A rental's `org_id` MUST be an org both renter and owner are active (and correctly un-blocked) members of; validated at request time via `isSharedOrganization`/`getSharedOrganizations`, no server-side "current org." | `TestRentalService_CreateRentalRequest` (tests/unit/rental_test.go:18) — "Success" (46), "Rejects an organization the caller is not a member of (SEC-RENTAL-001)" (73), "Blocked-flag business rule (owner lends, renter rents)" (99, 4 sub-subtests) | — | — | — | Complete (L1); Gap — L2, L3 | L2: `TestRentalService_CreateRentalRequest_Integration` (new, tests/integration/rental_ledger_test.go or a new file) — exercise `rentalService.CreateRentalRequest` (not a repo bypass) against a real DB with a caller/owner who don't share the requested org, assert `FAILED_PRECONDITION` and no `rentals` row created; add a second subtest for the DB-level `rentals_shared_org_check` CHECK constraint itself. L3: `TestRentalService_E2E` > "CreateRentalRequest rejects a non-shared organization" (new, tests/e2e/rental_test.go) — same scenario via the real gRPC client. | L1 coverage is thorough (shared-org membership + both directions of the blocked-flag rule). No integration or e2e test exercises the shared-org rejection path against a real DB/full stack. |
| FR-009 | `RejectRentalRequest` MUST require the rental to be `PENDING` (Known Discrepancy 2, RESOLVED). | `TestRentalService_RejectRentalRequest` (tests/unit/rental_test.go) — "Success on a PENDING rental", "Rejects a caller who is not the tool's owner", "Rejects a rental that is not PENDING (KD-2)" | — | `TestRentalService_E2E` > "Reject Rental Request" (tests/e2e/rental_test.go:47) — only exercises the PENDING happy path | Same e2e journey (happy path only) | Complete (L1); Gap — L3 for the rejection branch | L3: extend "Reject Rental Request" (tests/e2e/rental_test.go) with a subtest that approves a rental first, then attempts `RejectRentalRequest` on the now-APPROVED rental and asserts an error. | New FR carved out of the old Known Discrepancy 2 note; no L2. |
| FR-010 | `CancelRentalRequest` MUST require the rental to be `PENDING`/`APPROVED`/`SCHEDULED` (pre-pickup) (Known Discrepancy 3, RESOLVED). | `TestRentalService_CancelRental` (tests/unit/rental_test.go) — "Success on a PENDING rental", "Rejects a caller who is not the renter", "Rejects a rental that is already ACTIVE (KD-3)", "Rejects a rental that is already COMPLETED (KD-3)" | — | `TestRentalService_E2E` > "Cancel Rental Request" (tests/e2e/rental_test.go:153) — only exercises the PENDING happy path | Same e2e journey (happy path only) | Complete (L1); Gap — L3 for the rejection branch | L3: extend "Cancel Rental Request" (tests/e2e/rental_test.go) with a subtest that activates a rental first, then attempts `CancelRental` on the now-ACTIVE rental and asserts an error. | New FR carved out of the old Known Discrepancy 3 note; no L2. |

## Summary
- Total FRs: 10
- Complete: 8 (FR-002, FR-003, FR-004, FR-005; FR-001, FR-006, FR-009, FR-010 complete at L1/L3-happy-path with a named L2/L3 gap each)
- Gaps: 4 (FR-001 — L2 gap on the new overlap check; FR-006 — L3 gap on the new admin-grant branch; FR-007 — L3 gap; FR-008 — L2/L3 gap; FR-009/FR-010 — L3 gap on the new rejection branch)
- Unclassified: 0

**Gaps**: FR-001 (overlap check untested at L2 against real Postgres), FR-006 (org-admin grant untested at L3), FR-007 (no e2e coverage of `ListMyRentals`/`ListMyLendings`), FR-008 (shared-org rejection path untested at L2/L3), FR-009/FR-010 (non-PENDING/non-cancelable rejection paths untested at L3) — all planned tests proposed above.
