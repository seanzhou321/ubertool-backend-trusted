# Requirements Traceability Matrix — Rentals

- **Source spec**: `specs/005-rentals/spec.md`
- **Tier mapping in effect**: `sbr/README.md` → "This repo's adapter" (L1=`tests/unit`,
  L2=`tests/integration`, L3=`tests/e2e`, Grounding=`tests/smoke`+`tests/e2e` journeys)
- **Generated**: 2026-07-22 by `speckit-sbr-audit`; updated 2026-07-23 (Phase 4 re-audit);
  re-audited 2026-07-25 (FR-001/FR-004 Notes updated); FR-004 row updated again 2026-07-25 by
  `speckit-sbr-bugfix` after the accompanying `specs/005-rentals/spec.md` correction; FR-007
  added 2026-07-25 — see Summary
- **Mode**: retrofit audit (as-built) — see project constitution Principle I

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding (Smoke) | Boundary Status | Notes |
|---|---|---|---|---|---|---|---|
| FR-001 | `CreateRentalRequest` MUST validate `end_date > start_date`, MUST snapshot the tool's price fields onto the rental, MUST compute `total_cost_cents` from that snapshot. (Overlap rejection explicitly NOT required — Known Discrepancy 1.) | `TestRentalService_CreateRentalRequest` (`tests/unit/rental_test.go:45`) — 3 subtests as of 2026-07-23 22:11 (commit `c0b15cc`): success (happy path), rejects an end date that is not after the start date, rejects an organization the caller is not a member of (`"...(SEC-RENTAL-001)"`) | **Intentionally not separately tested** — see Notes | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:23`, via `doCreateRentalRequest`, reused by 3 more subtests) and `TestLedgerService_E2E > "Ledger Updates After Rental Completion"` (`tests/e2e/ledger_test.go:131`) — happy path only | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** The `end_date <= start_date` rejection clause (Acceptance Scenario 2) now has direct unit coverage. No bug found. L2 not added (2026-07-23 hardening pass): the e2e test already asserts the persisted price snapshot and `total_cost_cents` directly against Postgres — L3 subsumes L2 here. **2026-07-25 re-audit note**: commit `c0b15cc` (2026-07-23 22:11, after this file's own last update) added a renter-org-membership check to `CreateRentalRequest` (`internal/service/rental.go`; tracked as `SEC-RENTAL-001` in `sbr/rtm/009-security.rtm.md`) — a real business rule this RPC now enforces that FR-001's text doesn't name. Not in conflict with FR-001's stated MUST clauses (all three still hold and are tested); flagged as a candidate for its own FR if this domain's spec is ever revised, consistent with how FR-005/003 handles its own additive rank-check note. |
| FR-002 | `ApproveRentalRequest` MUST require the caller to be the tool's owner and the rental to be `PENDING`. | `TestRentalService_ApproveRentalRequest` (`tests/unit/rental_test.go`) — 3 subtests: success as owner on PENDING, rejects non-owner, rejects non-PENDING | **Intentionally not separately tested** — see Notes | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:31`) + 2 more subtests + `TestLedgerService_E2E > "Ledger Updates After Rental Completion"` (`tests/e2e/ledger_test.go:150`) — happy path | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** `TestRentalHandler_ApproveRentalRequest`'s handler-level mock still doesn't count as L1 evidence for the service's own gate, but a new direct service-level test now covers both reject clauses. No bug found. L2 not added (2026-07-23 hardening pass): the e2e test already exercises the real status transition against Postgres — L3 subsumes L2 here. |
| FR-003 | `FinalizeRentalRequest` MUST require the caller to be the renter and the rental to be `APPROVED`, and MUST set the tool's status to `RENTED` on success. | `TestRentalService_FinalizeRentalRequest` (`tests/unit/rental_test.go:252`) — 3 subtests: success (asserts tool-status-`RENTED` update via `mock.MatchedBy`), rejects non-renter caller, rejects non-`APPROVED` rental | **Intentionally not separately tested** — see Notes | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:34`, tool-status assertion at `:37`) + 2 more subtests + `TestLedgerService_E2E` (`tests/e2e/ledger_test.go:160`) — happy path only | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** Both MUST-reject clauses now unit-tested (Acceptance Scenario 6's non-`APPROVED` rejection included). No bug found. L2 not added (2026-07-23 hardening pass): the e2e test already asserts the real `tools.status` = `RENTED` update directly against Postgres — L3 subsumes L2 here. |
| FR-004 | `CompleteRental` MUST require the caller to be the tool's owner (corrected 2026-07-25 — was "a participant" prior to a 2026-07-23 security fix, see Notes) and the rental to be `ACTIVE`/`SCHEDULED`/`OVERDUE`, MUST recompute cost from the rental's own price snapshot (never the tool's current prices), and MUST only create ledger transactions / update balances when `charge_billsplit=true`. | `TestRentalService_CompleteRental` (`tests/unit/rental_test.go:78`) — subtests `"Success with charge_billsplit=true"` (`:111`), `"Success with charge_billsplit=false"` (`:144`), `"Settlement notification reminder text when charge_billsplit=false"` (`:176`), and, as of `c0b15cc`, `"Rejects a caller who is the renter, not the owner (SEC-RENTAL-005)"` (`:233`) | `TestRentalService_CompleteRental_Integration` (`tests/integration/rental_ledger_test.go:184`) — 5 subtests, calls `service.CompleteRental` directly against a real DB, including (as of `c0b15cc`) `"Rejects the renter — CompleteRental is owner-only"` (`:306`) | `TestRentalService_E2E > "Full Rental Lifecycle"` (`tests/e2e/rental_test.go:39`, charge_billsplit=true) and `"Full Rental Lifecycle with charge_billsplit=false"` (`:130`); also `TestLedgerService_E2E > "Ledger Updates After Rental Completion"` (`tests/e2e/ledger_test.go:184`) — happy path only, called as the owner | — | **Complete** | **Fixed 2026-07-22.** The prior "integration" test (`TestRentalAndLedger_Integration > "Full Lifecycle"`) bypassed `service.CompleteRental` entirely via direct repo writes, contradicting spec.md's "Current Test Coverage Baseline" claim of "(unit + integration)" coverage. Replaced with a new test that calls the real service method and covers: snapshot-vs-tool-price recomputation (tool's live price deliberately set to a different value), `charge_billsplit=true` balance/ledger effects, `charge_billsplit=false` no-op, non-owner reject, non-completable-status reject. No bug found — production behavior matched the spec exactly at the time. spec.md's Coverage Baseline corrected. **RESOLVED 2026-07-25 via `speckit-sbr-bugfix`** (root cause: missing/changed requirement, generalizable): commit `c0b15cc` (2026-07-23 22:11 — after this file's own last update at commit `40c51d4`, 16:46 the same day) had changed `loadAndValidateRental`'s caller gate (`internal/service/rental.go`, called from `CompleteRental`) from `rt.OwnerID != userID && rt.RenterID != userID` (participant) to `rt.OwnerID != userID` (owner-only), fixing `SEC-RENTAL-005` in `sbr/rtm/009-security.rtm.md` — the proto (`rental_service.proto`) had always documented `CompleteRental` as "owner only," and the renter-accepting code was the actual bug. Both the unit and integration test suites were updated in the same commit to assert the new owner-only behavior (cited above), and remain green (re-run 2026-07-25, `go test ./tests/unit/... -run TestRentalService_CompleteRental -v` — 4/4 subtests pass; L2 not re-run this session, no local Postgres reachable in this sandbox). A 2026-07-25 audit pass first caught the resulting spec.md/code mismatch (this row previously read "Complete (for current behavior — spec.md text now stale, see Note)"); `speckit-sbr-bugfix` then corrected `specs/005-rentals/spec.md`'s FR-004 text, Acceptance Scenario 3 (User Story 2), the Coverage Baseline paragraph, and added Known Discrepancy 5 documenting the fix's history — no further code or test change was needed, since the underlying behavior and its regression tests were already correct. |
| FR-005 | Every return-date-change transition (`ChangeRentalDates` active-rental case, `ApproveReturnDateChange`, `RejectReturnDateChange`, `AcknowledgeReturnDateRejection`, `CancelReturnDateChange`) MUST recompute `total_cost_cents` from the price snapshot whenever `end_date` changes. | `ChangeRentalDates`: `TestRentalService_ChangeRentalDates > "Renter Extension Active"` (`tests/unit/rental_test.go:373`), `> "Renter Updates Pending Extension Request"` (`:397`). `RejectReturnDateChange`: `TestRentalService_RejectReturnDateChange` (`:447`), 8 subtests. `AcknowledgeReturnDateRejection`: `TestRentalService_AcknowledgeReturnDateRejection` — 3 subtests. `CancelReturnDateChange`: `TestRentalService_CancelReturnDateChange` — 3 subtests. `ApproveReturnDateChange`: `TestRentalService_ApproveReturnDateChange` (`tests/unit/rental_test.go`) — 3 subtests: success sets `last_agreed_end_date` and activates (asserting `total_cost_cents` is left unchanged, since this RPC doesn't itself change `end_date`), rejects non-owner, rejects wrong status | `ChangeRentalDates` + `ApproveReturnDateChange`: `TestRentalDateChange_Integration > "Activate -> Extend -> Approve"` (`tests/integration/rental_ledger_test.go:230`). `RejectReturnDateChange`/`AcknowledgeReturnDateRejection`/`CancelReturnDateChange`: **intentionally not separately tested** — their e2e subtests (next column) already assert the recomputed `total_cost_cents` directly against Postgres, so L3 subsumes L2 for these three | `ChangeRentalDates` + `ApproveReturnDateChange`: `TestRentalService_E2E > "Update Extension Request While Pending (RETURN_DATE_CHANGED)"` (`tests/e2e/rental_test.go:65`). `RejectReturnDateChange` + `AcknowledgeReturnDateRejection`: `TestRentalService_E2E > "Reject Extension Request with Counter-Proposal, Renter Acknowledges"` (`tests/e2e/rental_test.go`) — owner counter-proposes a shorter end date (cost recomputed down), renter acknowledges (rolls back to `last_agreed_end_date`, cost recomputed again). `CancelReturnDateChange`: `TestRentalService_E2E > "Cancel Extension Request Before Owner Acts"` (`tests/e2e/rental_test.go`) — renter cancels a pending extension, rolling back to `last_agreed_end_date` | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 4).** `ApproveReturnDateChange`'s previously-missing unit test closed — reading `internal/service/rental.go:563` confirmed it does not itself mutate `end_date`/`total_cost_cents` (those were already applied by the preceding `ChangeRentalDates` call), so its test asserts cost is left unchanged rather than recomputed, which is the FR-consistent behavior ("whenever `end_date` changes" — it doesn't, here). The L2/L3 gap for `RejectReturnDateChange`/`AcknowledgeReturnDateRejection`/`CancelReturnDateChange` closed with 2 new e2e subtests exercising the full reject→acknowledge and cancel journeys against a real DB, confirming the rollback-to-`last_agreed_end_date` recompute end-to-end. No bug found. |
| FR-006 | `GetRental` MUST grant access to the rental's renter and owner. (As-built, admin access is explicitly NOT required — Known Discrepancy 4.) | `TestRentalService_GetRental` (`tests/unit/rental_test.go`) — 3 subtests: grants access to renter, grants access to owner, rejects any other caller | **Intentionally not separately tested** — see Notes | `TestRentalService_E2E > "GetRental grants access to renter and owner, rejects others"` (`tests/e2e/rental_test.go`) — all 3 clauses through the real gRPC handler against a live DB | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3, L1).** Previously the only occurrence of `GetRental` anywhere was a mock-interface stub. No bug found. **L3 added 2026-07-23 (hardening pass):** prior to this, `grep -r GetRental tests/e2e tests/integration` returned nothing. L2 not added: the new e2e test already exercises the real access-control read against Postgres — L3 subsumes L2 here. |
| FR-007 | `ListMyRentals`/`ListMyLendings` MUST OR-combine a given `status` array (empty array = all statuses) and MUST treat `organization_id` as optional (filters when supplied, spans all the caller's orgs when `0`). | `TestRentalService_ListMyRentalsAndLendings` (`tests/unit/rental_test.go`) — 4 subtests: `ListRentals`/`ListLendings` each forward the caller as renterID/ownerID with the given status filter and pagination unchanged, and each forward an empty status filter unchanged (not translated into a narrowing filter) | `TestRentalRepository_ListByRenterAndOwner_OrgFilter` (`tests/integration/rental_list_test.go`, new file) — real DB, 4 subtests: a specific `organization_id` filters correctly for both `ListByRenter`/`ListByOwner`; `organization_id = 0` returns rentals/lendings across two different orgs for the same caller | — | — | **Complete** | **Added 2026-07-25** (new FR — these two RPCs were previously described only in User Story 4 Acceptance Scenario 2, with no `FR-XXX` entry or RTM row at all; `spec.md`'s own "Not covered anywhere" list already self-flagged the underlying OR-combination test gap, found during a `docs/design/grpc_api_business_logic.md`-vs-RTM alignment check). Investigating the repository SQL to write this FR surfaced a real bug: `organization_id` was never treated as optional, contrary to `docs/design/grpc_api_business_logic.md`. **Confirmed a real bug and fixed same day** per explicit product decision (`organization_id` MUST be optional) — see Known Discrepancy 6. **L1 closed**: `rentalService.ListRentals`/`ListLendings` are thin pass-throughs to `rentalRepo.ListByRenter`/`ListByOwner` (`internal/service/rental.go:1053-1059`) — the OR-combination itself is a SQL `status IN (...)` clause, not service-layer logic, so the L1 test's scope is to lock in that the service forwards arguments unchanged. **L2 closed with the fix**: both queries (`internal/repository/postgres/rental.go`) now build `WHERE renter_id = $1`/`owner_id = $1` and append `AND org_id = $N` only when `orgID > 0`; the new integration test was run red against the pre-fix code (confirmed `organization_id = 0` returned 0 rows instead of 2) before the fix, then green after. Full unit and integration suites re-run green. **L3 not added this session**: the running e2e server (a podman container) would need rebuilding to pick up this fix, deferred pending confirmation before restarting a running service; the handler (`internal/api/grpc/rental.go`) forwards `req.OrganizationId` unchanged with no additional logic of its own, so L2 is expected to already subsume what an e2e test would add here — flag for a follow-up e2e subtest once the server is next rebuilt for another reason. The OR-combination's own real-DB semantics (`status IN (...)`) remain unverified by a dedicated test — this row's L2 evidence exercises the `org_id` predicate specifically, not the status predicate; a follow-up integration subtest exercising multiple statuses would close that separately. |

## Summary

- **7 FR-IDs audited, all 7 fully `Complete`** (FR-004 closed 2026-07-22 as SBR remediation
  Phase 1; FR-002, FR-003 closed 2026-07-23 as Phase 2; FR-001, FR-006 closed 2026-07-23 as
  Phase 3; FR-005 closed 2026-07-23 as Phase 4; FR-007 added and closed 2026-07-25, including a
  real bug fix).
- **Remaining gaps**: none tracked against this FR set. Two narrower follow-ups noted in
  FR-007's own Notes (not blocking `Complete`): an e2e subtest for the `organization_id` fix
  once the e2e server is next rebuilt, and a dedicated integration subtest for the
  status-array OR-combination itself (this pass's L2 evidence exercised the `org_id`
  predicate, not the `status IN (...)` predicate).
- **Phase 1 outcome**: FR-004's spec-vs-test discrepancy resolved — the fake integration test
  (direct repo writes bypassing `service.CompleteRental`) replaced with a real one exercising
  snapshot-recomputation, the `charge_billsplit` gate, and the participant/status reject
  clauses. No bug found. spec.md's Coverage Baseline corrected.
- **Phase 2 outcome**: FR-002's owner/`PENDING` reject clauses and FR-003's renter/`APPROVED`
  reject clauses unit-tested (both previously had zero direct-service-level coverage — only
  handler-mocked or e2e-happy-path evidence existed). No bugs found in either.
- **Phase 3 outcome**: FR-001's date-order rejection unit-tested. FR-005's two previously
  zero-coverage sub-RPCs (`AcknowledgeReturnDateRejection`, `CancelReturnDateChange`) now have
  full unit coverage including their recompute-cost assertions. FR-006's `GetRental` (previously
  only a mock-interface stub) unit-tested for both grant clauses and the reject case. No bugs
  found in any of the three.
- **Phase 4 outcome**: FR-005's remaining gaps closed — `ApproveReturnDateChange`'s missing unit
  test added (3 subtests), and the L2/L3 gap for `RejectReturnDateChange`/
  `AcknowledgeReturnDateRejection`/`CancelReturnDateChange` closed with 2 new e2e subtests
  exercising the full reject→acknowledge and cancel journeys against a real DB. No bug found.
- **Phase 4 hardening pass (2026-07-23)**: re-audited every cell for unexplained blanks. Closed
  FR-006's genuine L3 gap — `GetRental` had never once been called through the real gRPC handler
  against a live DB (`grep -r GetRental tests/e2e tests/integration` returned nothing) — with a
  new e2e test covering all 3 access clauses (renter grant, owner grant, third-party reject). No
  bug found. Every other remaining blank (FR-001/FR-002/FR-003's L2, and 3 of FR-005's 5
  sub-RPCs' L2) documented as a legitimate omission: each one's e2e evidence already asserts the
  same real-DB effect a separate L2 test would check.
- **Unclassified**: none — every row above was resolved to either cited evidence or an
  explicit, named gap.
- **2026-07-25 re-audit**: re-verified all 6 FR-IDs against the current codebase. Found that
  commit `c0b15cc` ("security auditing, 1st pass," 2026-07-23 22:11) landed **after** this
  file's own last update (`40c51d4`, same day 16:46) and changed two behaviors:
  - `CreateRentalRequest` (FR-001) gained a renter-org-membership check (`SEC-RENTAL-001`) —
    additive, does not contradict FR-001's text; Notes updated, Boundary Status unchanged.
  - `CompleteRental` (FR-004) changed from accepting "a participant" (owner or renter) to
    owner-only (`SEC-RENTAL-005`), fixing a real bug where the code contradicted its own proto
    doc comment. Test coverage for the new (correct) behavior is solid and current at all three
    tiers — but FR-004's own requirement text ("a participant caller") was, at the time of this
    2026-07-25 audit pass, an inaccurate description of the system. **Resolved same day via
    `speckit-sbr-bugfix`**: `specs/005-rentals/spec.md`'s FR-004, Acceptance Scenario 3 (User
    Story 2), and Coverage Baseline paragraph were corrected to describe the owner-only
    behavior as current and intended, and a new Known Discrepancy 5 documents the fix's
    history. No code or test change was needed — see FR-004's Notes for the full trace.
  All other FRs (002, 003, 005, 006) and their cited tests are unchanged and re-confirmed still
  present by name. No new gaps in the existing FR set.
- **FR-007 added and closed 2026-07-25**: `ListMyRentals`/`ListMyLendings` — self-flagged in
  `spec.md`'s own "Not covered anywhere" list as untested for their OR-status-filter logic,
  with no `FR-XXX` entry or RTM row of their own — were formalized as FR-007. `grep`-confirmed
  **zero** test coverage at any tier for either RPC beforehand. Investigating the repository
  SQL to write this FR surfaced a real bug: `organization_id` was always a mandatory
  exact-match filter, never optional as `docs/design/grpc_api_business_logic.md` documents.
  **Confirmed a real bug per explicit product decision** (`organization_id` MUST be optional,
  2026-07-25) and **fixed same day**: `internal/repository/postgres/rental.go`'s
  `ListByRenter`/`ListByOwner` now only apply `AND org_id = $N` when `orgID > 0`. L1 closed
  with `TestRentalService_ListMyRentalsAndLendings` (4 subtests, `tests/unit/rental_test.go`,
  scoped to the service's pass-through role). L2 closed with
  `TestRentalRepository_ListByRenterAndOwner_OrgFilter` (`tests/integration/rental_list_test.go`,
  new file, real DB against the Postgres instance in `podman/trusted-group/postgres`) — run red
  against the pre-fix code first (confirmed `organization_id = 0` returned 0 rows instead of
  the expected 2), then green after the fix. Full unit and integration suites re-run green. See
  new Known Discrepancy 6 in `spec.md` for the fix write-up. L3 and the OR-combination's own
  dedicated test remain open as narrower follow-ups (see FR-007's Notes).
- **Still open (not FR-007, tracked here for visibility)**: `CancelRentalRequest` and
  `RejectRentalRequest`'s core business logic each have only a `Gap` row in the security RTM
  (`SEC-RENTAL-003`/`004`, tracking their self-documented missing status guard, Known
  Discrepancy 3 in this domain's `spec.md`) and no functional `FR-XXX` here — out of scope for
  this pass, which was limited to `ValidateInvite`/`ListMyOrganizations`/`ListJoinRequests`/
  `ListMyRentals`/`ListMyLendings`.
