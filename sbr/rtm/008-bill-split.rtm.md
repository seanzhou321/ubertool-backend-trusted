# Requirements Traceability Matrix — Bill Split

- **Source spec**: `specs/008-bill-split/spec.md`
- **Tier mapping in effect**: `sbr/README.md` → "This repo's adapter" (L1=`tests/unit`,
  L2=`tests/integration`, L3=`tests/e2e`, Grounding=`tests/smoke`+`tests/e2e` journeys)
- **Generated**: 2026-07-22 by `speckit-sbr-audit`; updated 2026-07-23 (Phase 4 re-audit)
- **Mode**: retrofit audit (as-built) — see project constitution Principle I

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding (Smoke) | Boundary Status | Notes |
|---|---|---|---|---|---|---|---|
| FR-001 | Monthly settlement job: snapshot balances, compute net transactions via largest-balance-first greedy match, create one `PENDING` bill per transaction, skip already-settled pairings. | `TestCalculateTransactions` (`tests/unit/billing_jobs_test.go:9`), `TestCalculateTransactions_EdgeCase` (`:114`) — netting algorithm only | `TestPerformBillSplittingForOrg` (`tests/integration/billing_jobs_integration_test.go:15`), `TestPerformBillSplittingForOrg_BelowThreshold` (`:72`) — mocked DB, bill creation + threshold-stop; `TestTakeBalanceSnapshots` (`tests/integration/billing_jobs_realdb_test.go`) — real DB, 2 subtests: snapshots the current balance, and skips an already-settled pairing on a second run without duplicating or overwriting | `TestBillSplitAlgorithm_E2E` (`tests/e2e/bill_split_test.go:610`) — real DB, verifies 4 resulting bill rows/amounts | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** `TakeBalanceSnapshots` and the "skip already-settled pairing" `ON CONFLICT DO NOTHING` clause were the two zero-coverage MUST-clauses; both now closed with a real-DB integration test. No bug found. |
| FR-002 | System MUST email both debtor and creditor when a bill is created; MUST record `notice_sent_at` only once the debtor's email succeeds. | **N/A** — see Notes | `TestSendBillSplittingNotices` (`tests/integration/notification_jobs_test.go`) — real DB + capturing email mock: asserts both debtor and creditor are emailed and `notice_sent_at` is recorded | **N/A** — see Notes | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** No bug found. **L1/L3 documented as N/A (2026-07-23 hardening pass):** `SendBillSplittingNotices` (`internal/jobs/notification_jobs.go`) is a cron job that mixes raw SQL queries directly against `*sql.DB` with the email interface — there is no repository abstraction to mock, so L1 is not constructable without a production-code refactor out of scope here. It also has no gRPC/proto surface (triggered only by the scheduler, per `config.yaml`'s `send_bill_notices` entry) — this project's adapter defines L3 as "the gRPC/proto surface," which doesn't exist for this job, so L3 is structurally inapplicable, not merely skipped. |
| FR-003 | System MUST send a reminder email to both parties of a `PENDING` bill once `notice_sent_at` is more than 72 hours in the past. | **N/A** — see Notes | `TestSendBillReminders` (`tests/integration/notification_jobs_test.go`) — real DB: a bill with `notice_sent_at` 4 days old reminds both parties; a bill with `notice_sent_at` 1 hour old does not | **N/A** — see Notes | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** No bug found. **L1/L3 documented as N/A (2026-07-23 hardening pass):** same reasoning as FR-002 — `SendBillReminders` is a raw-SQL cron job with no gRPC surface. |
| FR-004 | System MUST automatically transition a `PENDING` bill to `DISPUTED` (with dispute reason + `DISPUTE_OPENED` action) once `notice_sent_at` is more than 10 days in the past. | **N/A** — see Notes | `TestCheckOverdueBills` (`tests/integration/billing_jobs_realdb_test.go`) — real DB: a bill overdue by 11 days transitions to `DISPUTED` with `dispute_reason='DEBTOR_NO_ACK'` and a `DISPUTE_OPENED` bill_action; a bill within the 10-day window (2 days) is left `PENDING` | **N/A** — see Notes | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** This job calls a real Postgres function (`check_overdue_bills()`), so it needed a real-DB test rather than a mock. No bug found. **L1/L3 documented as N/A (2026-07-23 hardening pass):** confirmed by reading `internal/jobs/billing_jobs.go:13` — `CheckOverdueBills` is a 1-line direct call (`jr.db.ExecContext(ctx, "SELECT check_overdue_bills()")`) with no Go-level business logic to unit-test in isolation, and no gRPC surface to exercise at L3. |
| FR-005 | System MUST automatically transition a still-`DISPUTED` bill at month-end to `SYSTEM_DEFAULT_ACTION`/`BOTH_FAULT`, blocking debtor (renting) and creditor (lending), with **no** balance penalty. | **N/A** — see Notes | `TestResolveDisputedBills` (`tests/integration/billing_jobs_realdb_test.go`) — real DB: a DISPUTED bill transitions to `SYSTEM_DEFAULT_ACTION`/`BOTH_FAULT`, debtor's `renting_blocked` and creditor's `lending_blocked` are set, a `SYSTEM_AUTO_RESOLVE` bill_action is recorded, and — the behaviorally unique part — both parties' `balance_cents` are asserted unchanged | **N/A** — see Notes | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** This job calls a real Postgres function (`auto_resolve_disputed_bills()`), so it needed a real-DB test. The no-balance-penalty distinction from FR-009's admin-driven `BOTH_FAULT` path is now explicitly regression-locked. No bug found. **L1/L3 documented as N/A (2026-07-23 hardening pass):** same reasoning as FR-004 — `ResolveDisputedBills` iterates orgs and calls a real Postgres function per org, with no gRPC surface. |
| FR-006 | `AcknowledgePayment` MUST accept debtor/creditor calls while status is `PENDING` or `DISPUTED`, MUST reject any other status, MUST reject double-acknowledgment, MUST reject creditor-before-debtor. | `TestBillSplitService_AcknowledgePayment` (`tests/unit/bill_split_service_test.go:201`) — 8 subtests: debtor/creditor success from `PENDING`, success from `DISPUTED`, all 3 reject clauses, uninvolved-caller reject | `TestBillSplitService_Integration > "Full Bill Lifecycle"` (`tests/integration/bill_split_test.go:85`) | `TestBillSplitService_PaymentAcknowledgment` (`tests/e2e/bill_split_test.go:23`) | — | **Complete** | **Fixed 2026-07-22.** Replaced the deferral comment ("complex balance update logic... better tested in E2E") with real unit coverage — the e2e/integration tests it deferred to only ever exercised the `PENDING`-origin happy path, so all 3 MUST-reject clauses were actually untested anywhere. No bug found; all reject clauses behaved exactly as documented. |
| FR-007 | When both parties have acknowledged: status → `PAID`, `resolution_outcome = GRACEFUL`, balance transfer, notify debtor — regardless of whether the bill reached this point via `PENDING` or `DISPUTED`. | `TestBillSplitService_AcknowledgePayment > "Creditor acknowledgment succeeds from DISPUTED (not just PENDING)"` (`tests/unit/bill_split_service_test.go`) — asserts status→PAID, resolution_outcome=GRACEFUL, `ResolvedAt` set, balance transfer, AND the notify-debtor clause (in-app `Dispatch` + `SendBillReceiptConfirmation` email) | `"Full Bill Lifecycle"` (`tests/integration/bill_split_test.go:85`) — `PENDING`-origin only | `TestBillSplitService_PaymentAcknowledgment` (`tests/e2e/bill_split_test.go:23-100`) — `PENDING`-origin only | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** The `DISPUTED`-origin subtest already existed but only asserted `err == nil`; expanded it to assert every clause the FR names, including the previously-unasserted notify-debtor clause. No bug found — production behavior already matched the spec (User Story 2 Scenario 3 / SC-003 is now genuinely met, not just claimed). |
| FR-008 | `ResolveDispute` MUST be restricted to `ADMIN`/`SUPER_ADMIN` who are not the debtor/creditor, and MUST only operate on `DISPUTED` bills. | `TestBillSplitService_ResolveDispute > "Error_NotDisputed"` (`tests/unit/bill_split_service_test.go:449`), `> "Error_AdminInvolved"` (`:467`) | `"Dispute Lifecycle"` (`tests/integration/bill_split_test.go:139`) — admin-caller happy path only | `TestBillSplitService_AdminResolvesDisputeDebtorFault` (`tests/e2e/bill_split_test.go:170`) and siblings — happy paths; `TestBillSplitService_ResolveDispute_RejectionPaths` (`tests/e2e/bill_split_test.go`) — 3 subtests: rejects a non-admin caller, rejects an admin who is a party, rejects a non-DISPUTED bill (all via `resp.Success=false`, matching the handler's report-via-response pattern, not a gRPC error) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** Closed the one genuinely separate gap: the contract-level (e2e) surface now exercises both rejection paths. No bug found — confirmed the handler reports these as `Success=false` responses rather than gRPC errors (same pattern as `AcknowledgePayment`). |
| FR-009 | `ResolveDispute` MUST apply the documented balance/blocking consequences for each of `DEBTOR_FAULT`, `CREDITOR_FAULT`, `BOTH_FAULT`, `GRACEFUL`, and MUST notify both parties. | `"Success_DebtorFault"` (`tests/unit/bill_split_service_test.go:280`), `"Success_CreditorFault"` (`:345`), `"Success_BothFault"` (`:393`), `"Success_Graceful"` (`:611`) — all 4 outcomes' balance/blocking + notification-dispatch + email-service calls asserted | `"Dispute Lifecycle"` (`tests/integration/bill_split_test.go:139`) — `DEBTOR_FAULT` only | `TestBillSplitService_AdminResolvesDisputeDebtorFault` (`:170`), `...CreditorFault` (`:230`), `...BothAtFault` (`:283`) — 3 of 4 outcomes; no `GRACEFUL` e2e test | — | **Complete** | **Fixed 2026-07-22.** `GRACEFUL` — the one outcome explicitly named in spec.md's SC-001 with zero prior coverage — is now unit-tested, confirming the admin-driven `ResolveDispute` GRACEFUL path (distinct from `AcknowledgePayment`'s own mutual-acknowledgment GRACEFUL path — see FR-006/FR-007): full balance transfer, no penalty, resolves to `ADMIN_RESOLVED`. No bug found. SC-001 corrected in spec.md. L3 `GRACEFUL` coverage remains absent but L1 is sufficient to close the spec-vs-test contradiction that was the actual finding here. |
| FR-010 | `ListPayments` MUST filter to the caller's own bills in the given org, using `show_history` to toggle active vs. completed statuses. | `TestBillSplitService_ListPayments > "Success_ShowHistory"` (`tests/unit/bill_split_service_test.go:80`), `> "Success_NoHistory"` (`:99`), `> "Error_NotMember"` (`:115`) | **Intentionally not separately tested** — see Notes | `TestBillSplitService_ListPayments` (`tests/e2e/bill_split_test.go:443`) — asserts active list (`PENDING`+`DISPUTED`) vs. history list (`PAID`) counts against a real DB | — | **Complete** | No L2 test exercises this RPC directly, but the FR's core clause (own-bills scoping + `show_history` toggle) is proven both against the real service interface (L1, including the not-a-member rejection) and end-to-end through the real gRPC/DB stack (L3) — per `sbr/README.md`, not every requirement needs every tier, and L3 here already subsumes what an L2 test would add. |
| FR-011 | `GetPaymentDetail` MUST be restricted to the bill's debtor, creditor, or an org admin, and MUST compute `can_acknowledge` per User Story 2's rules. | `TestBillSplitService_GetPaymentDetail` (`tests/unit/bill_split_service_test.go:135`) — 7 subtests: `Success_AsDebtor`, `Success_AsCreditor`, `Error_NotInvolved`, `Error_BillNotFound`, `Success_AsOrgAdmin_NotAParty` (the third authorized-caller class), and 3 `CanAcknowledge_False_*` subtests (debtor already acknowledged, creditor-before-debtor, wrong status) | **Intentionally not separately tested** — see Notes | Within `TestBillSplitService_PaymentAcknowledgment` (`tests/e2e/bill_split_test.go:52-58`); within `TestBillSplitService_UnauthorizedAccess` (`:592-595`) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** The org-admin (non-party) authorized-caller class and all `can_acknowledge=false` branches are now exercised — previously only the two `true` cases were ever asserted. No bug found. L2 not added (2026-07-23 hardening pass): the authorization/`can_acknowledge` logic is already thoroughly proven at L1 (7 subtests against a real repo interface) and reinforced at L3 against a real DB — a separate L2 test would gain little additional confidence, consistent with this file's own FR-010 reasoning. |
| FR-012 | `ListDisputedPayments`/`ListResolvedDisputes` MUST be restricted to org admins; `ListDisputedPayments` MUST exclude bills the calling admin is a party to. | `TestBillSplitService_ListDisputedPayments > "Success"` (`tests/unit/bill_split_service_test.go:207`), `> "Error_NotAdmin"` (`:222`); `TestBillSplitService_ListResolvedDisputes > "Success"` (`:243`, no rejection subtest) | `"Dispute Lifecycle"` (`tests/integration/bill_split_test.go:139`); `TestBillRepository_ListDisputedByOrg_ExcludesAdminParty` (`tests/integration/bill_repository_test.go`) — real DB adversarial fixture: a disputed bill where the admin IS the debtor, one where the admin IS the creditor, and one where the admin is uninvolved; asserts the first two are excluded and the third is included | `TestBillSplitService_DebtorDisputesPayment` (`tests/e2e/bill_split_test.go:108`) for `ListDisputedPayments`; `TestBillSplitService_ListResolvedDisputes` (`:507`) — admin-caller happy paths only | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2).** The exclusion clause is a real SQL `WHERE debtor_user_id != $3 AND creditor_user_id != $3` condition, so it needed a real-DB adversarial fixture rather than a mocked pass-through assertion. No bug found — the exclusion worked correctly in both directions (admin-as-debtor, admin-as-creditor). `ListResolvedDisputes`'s missing rejection test remains a minor, lower-severity gap not tracked as part of this FR's core clause. |
| FR-013 | As-built, `ListPayments`/`ListDisputedPayments`/`ListResolvedDisputes` MUST return the full unfiltered/unpaginated result set — pagination and `settlement_month`/`resolution_outcome` filters are accepted but ignored. | **Intentionally not separately tested** — see Notes | **Intentionally not separately tested** — see Notes | `TestBillSplitService_IgnoredPaginationAndFilters` (`tests/e2e/bill_split_test.go`) — populates `Pagination{Page:1, PageSize:1}` on all 3 RPCs, plus a non-matching `settlement_month` on `ListPayments` and a non-matching `resolution_outcome` on `ListResolvedDisputes`, and asserts the full result set is still returned | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 4).** The as-built "ignored" behavior that Known Discrepancy 1 and SC-005 hinge on is now regression-locked — a future accidental implementation of real filtering/pagination would be caught as a test failure, correctly signaling the behavior changed on purpose. No bug found. **L1/L2 not added (2026-07-23 hardening pass):** confirmed by reading `BillSplitHandler.ListPayments` (`internal/api/grpc/bill_split.go:74`) that the handler itself never reads `req.SettlementMonth`/`req.Pagination` before calling the service — the fields are dropped at the handler boundary. A handler-level L1 test could prove this mechanism, but would require building a full `MockBillSplitService` (no such mock exists yet for this large interface) for marginal benefit beyond what the e2e test's outcome-level assertion already proves; not undertaken in this pass. |

## Summary

- **13 FR-IDs audited, all 13 fully `Complete`** (FR-006, FR-009, FR-010 closed 2026-07-22 as
  SBR remediation Phase 1; FR-001, FR-004, FR-005, FR-007, FR-012 closed 2026-07-23 as Phase 2;
  FR-002, FR-003, FR-008, FR-011 closed 2026-07-23 as Phase 3; FR-013 closed 2026-07-23 as
  Phase 4).
- **Remaining gaps**: none.
- **Phase 1 outcome**: FR-006's zero-coverage reject clauses closed (8 new subtests, no bug
  found — the deferral-to-e2e comment's implicit promise was never actually fulfilled by the
  e2e file it pointed to). FR-009's SC-001 spec-vs-test contradiction resolved — the `GRACEFUL`
  outcome of admin-driven `ResolveDispute` (distinct from `AcknowledgePayment`'s own GRACEFUL
  path) now has a dedicated unit test; no bug found. spec.md's SC-001 corrected.
- **Phase 2 outcome**: FR-001's `TakeBalanceSnapshots` and skip-already-settled clause, FR-004's
  auto `PENDING→DISPUTED` job, and FR-005's auto `DISPUTED→SYSTEM_DEFAULT_ACTION` job all closed
  with real-DB integration tests (each calls a real Postgres function or DB-level `ON CONFLICT`
  semantic that a mock cannot meaningfully exercise). FR-007's `DISPUTED`-origin graceful path
  expanded from an `err == nil`-only assertion to cover every clause the FR names, including the
  previously-unasserted notify-debtor clause. FR-012's admin-is-a-party exclusion closed with a
  real adversarial fixture (admin as debtor, admin as creditor, admin uninvolved). No bugs found
  in any of the five.
- **Phase 3 outcome**: FR-002's notice email and FR-003's reminder email jobs closed with
  real-DB integration tests (a capturing `EmailService` wrapper records which addresses were
  emailed, since both jobs mix raw SQL queries with the email interface). FR-008's `ResolveDispute`
  contract-level rejection paths closed at e2e — confirmed the handler reports rejections as
  `Success=false` responses, not gRPC errors, matching `AcknowledgePayment`'s existing pattern.
  FR-011's org-admin (non-party) authorized-caller class and all `can_acknowledge=false`
  branches closed. No bugs found in any of the four.
- **Phase 4 outcome**: FR-013's as-built ignored pagination/filter fields regression-locked with
  an e2e test across all 3 affected RPCs. No bug found.
- **Phase 4 hardening pass (2026-07-23)**: re-audited every cell for unexplained blanks. No new
  tests were warranted here — every remaining blank turned out to be a legitimate omission:
  FR-002/FR-003/FR-004/FR-005's L1/L3 are structurally inapplicable (these are cron jobs mixing
  raw SQL against `*sql.DB` with no repository abstraction to mock, and no gRPC/proto surface —
  confirmed by reading `internal/jobs/billing_jobs.go` and `internal/jobs/notification_jobs.go`);
  FR-010/FR-011's L2 is already subsumed by thorough L1+L3 coverage of the same authorization/
  scoping logic; FR-013's L1/L2 would require building a new `MockBillSplitService` for a
  handler-level test proving a mechanism the e2e test's outcome-level assertion already
  regression-locks. All rows updated with explicit Notes justification; no new gaps found.
- **Unclassified**: none — every row above was resolved to either cited evidence or an
  explicit, named gap.
