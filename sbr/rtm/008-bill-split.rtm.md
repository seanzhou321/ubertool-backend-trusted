# Requirements Traceability Matrix — Bill Split

- **Source spec**: `specs/008-bill-split/spec.md`
- **Tier mapping in effect**: `sbr/README.md` → "This repo's adapter" (L1=`tests/unit`,
  L2=`tests/integration`, L3=`tests/e2e`, Grounding=`tests/smoke`+`tests/e2e` journeys)
- **Generated**: 2026-07-22 by `speckit-sbr-audit`
- **Mode**: retrofit audit (as-built) — see project constitution Principle I

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding (Smoke) | Boundary Status | Notes |
|---|---|---|---|---|---|---|---|
| FR-001 | Monthly settlement job: snapshot balances, compute net transactions via largest-balance-first greedy match, create one `PENDING` bill per transaction, skip already-settled pairings. | `TestCalculateTransactions` (`tests/unit/billing_jobs_test.go:9`), `TestCalculateTransactions_EdgeCase` (`:114`) — netting algorithm only | `TestPerformBillSplittingForOrg` (`tests/integration/billing_jobs_integration_test.go:15`), `TestPerformBillSplittingForOrg_BelowThreshold` (`:72`) — mocked DB, bill creation + threshold-stop only | `TestBillSplitAlgorithm_E2E` (`tests/e2e/bill_split_test.go:610`) — real DB, verifies 4 resulting bill rows/amounts | — | **Gap — L1 (partial), L2 (partial), L3 (partial)** | The netting/threshold/bill-creation clause is well covered across all three tiers. But two of the FR's MUST clauses have **zero** evidence at any tier: (1) `TakeBalanceSnapshots` — `grep -r TakeBalanceSnapshots tests/` returns nothing; (2) the "skip already-settled pairing" (`ON CONFLICT DO NOTHING`) clause — no test re-runs the job against a month that already produced a bill and asserts no duplicate/error, despite spec.md's own Edge Cases section calling this out explicitly. |
| FR-002 | System MUST email both debtor and creditor when a bill is created; MUST record `notice_sent_at` only once the debtor's email succeeds. | — | — | — | — | **Gap — all tiers** | `grep -r SendBillSplittingNotices tests/` returns nothing. `notice_sent_at` appears only as a read-only field mapped by `tests/e2e/helpers.go:375` (`GetBillByID` helper) — no test body ever asserts on it. This matches spec.md's own "Current Test Coverage Baseline" self-disclosure verbatim. |
| FR-003 | System MUST send a reminder email to both parties of a `PENDING` bill once `notice_sent_at` is more than 72 hours in the past. | — | — | — | — | **Gap — all tiers** | `grep -r SendBillReminders tests/` returns nothing. No test seeds a bill with an aged `notice_sent_at` and runs the reminder job. |
| FR-004 | System MUST automatically transition a `PENDING` bill to `DISPUTED` (with dispute reason + `DISPUTE_OPENED` action) once `notice_sent_at` is more than 10 days in the past. | — | — | — | — | **Gap — all tiers** | `grep -r CheckOverdueBills tests/` and `grep -r check_overdue_bills tests/` both return nothing. Every `DISPUTED`-status bill used elsewhere in the test suite (e.g. `tests/e2e/bill_split_test.go:138-144`) is created by directly `UPDATE`-ing the row in the test, never by exercising this job. This independently confirms the prior exploration pass's finding: the auto-dispute-transition behavior has no matching test at any tier. |
| FR-005 | System MUST automatically transition a still-`DISPUTED` bill at month-end to `SYSTEM_DEFAULT_ACTION`/`BOTH_FAULT`, blocking debtor (renting) and creditor (lending), with **no** balance penalty. | — | — | — | — | **Gap — all tiers** | `grep -r ResolveDisputedBills tests/` and `grep -r auto_resolve_disputed_bills tests/` both return nothing. This is a distinct code path from the admin-driven `BOTH_FAULT` resolution (FR-009), which *does* apply a balance penalty — the no-penalty distinction that makes this path behaviorally unique is exactly the part with zero coverage. |
| FR-006 | `AcknowledgePayment` MUST accept debtor/creditor calls while status is `PENDING` or `DISPUTED`, MUST reject any other status, MUST reject double-acknowledgment, MUST reject creditor-before-debtor. | `TestBillSplitService_AcknowledgePayment` (`tests/unit/bill_split_service_test.go:201`) — 8 subtests: debtor/creditor success from `PENDING`, success from `DISPUTED`, all 3 reject clauses, uninvolved-caller reject | `TestBillSplitService_Integration > "Full Bill Lifecycle"` (`tests/integration/bill_split_test.go:85`) | `TestBillSplitService_PaymentAcknowledgment` (`tests/e2e/bill_split_test.go:23`) | — | **Complete** | **Fixed 2026-07-22.** Replaced the deferral comment ("complex balance update logic... better tested in E2E") with real unit coverage — the e2e/integration tests it deferred to only ever exercised the `PENDING`-origin happy path, so all 3 MUST-reject clauses were actually untested anywhere. No bug found; all reject clauses behaved exactly as documented. |
| FR-007 | When both parties have acknowledged: status → `PAID`, `resolution_outcome = GRACEFUL`, balance transfer, notify debtor — regardless of whether the bill reached this point via `PENDING` or `DISPUTED`. | — | `"Full Bill Lifecycle"` (`tests/integration/bill_split_test.go:85`) — `PENDING`-origin only; status/balance asserted, no notify-debtor assertion | `TestBillSplitService_PaymentAcknowledgment` (`tests/e2e/bill_split_test.go:23-100`) — `PENDING`-origin only; status, balance, and `bill_actions` count/latest-action asserted, no notify-debtor assertion | — | **Gap — L1, L2 (partial), L3 (partial)** | The `DISPUTED`-origin graceful-resolution path (User Story 2 Scenario 3 / SC-003) has **zero** test evidence at any tier — confirmed by grep and matching spec.md's own baseline section and its own SC-003 requirement, which is therefore currently unmet by the test suite. The "notify the debtor" clause is also never explicitly asserted (no email/notification-dispatch check in either the L2 or L3 test bodies). |
| FR-008 | `ResolveDispute` MUST be restricted to `ADMIN`/`SUPER_ADMIN` who are not the debtor/creditor, and MUST only operate on `DISPUTED` bills. | `TestBillSplitService_ResolveDispute > "Error_NotDisputed"` (`tests/unit/bill_split_service_test.go:449`), `> "Error_AdminInvolved"` (`:467`) — explicit rejection-path tests with error-message assertions | `"Dispute Lifecycle"` (`tests/integration/bill_split_test.go:139`) — admin-caller happy path only | `TestBillSplitService_AdminResolvesDisputeDebtorFault` (`tests/e2e/bill_split_test.go:170`) and siblings — admin-caller happy paths only; no rejection subtest anywhere in the e2e file | — | **Gap — L3** | L1 is thorough and directly verifies both MUST-reject clauses. Following the same reasoning as the 003 RTM's FR-005 finding: an authorization gate proven at L1 against the real service interface gains little from a redundant L2 rejection test, so L2 is not flagged — but the contract-level (e2e/L3) surface never exercises either rejection path, which is a genuine, separate gap at the tier meant to stand in for consumer-driven contract tests (Constitution Principle VI). |
| FR-009 | `ResolveDispute` MUST apply the documented balance/blocking consequences for each of `DEBTOR_FAULT`, `CREDITOR_FAULT`, `BOTH_FAULT`, `GRACEFUL`, and MUST notify both parties. | `"Success_DebtorFault"` (`tests/unit/bill_split_service_test.go:280`), `"Success_CreditorFault"` (`:345`), `"Success_BothFault"` (`:393`), `"Success_Graceful"` (`:611`) — all 4 outcomes' balance/blocking + notification-dispatch + email-service calls asserted | `"Dispute Lifecycle"` (`tests/integration/bill_split_test.go:139`) — `DEBTOR_FAULT` only | `TestBillSplitService_AdminResolvesDisputeDebtorFault` (`:170`), `...CreditorFault` (`:230`), `...BothAtFault` (`:283`) — 3 of 4 outcomes; no `GRACEFUL` e2e test | — | **Complete** | **Fixed 2026-07-22.** `GRACEFUL` — the one outcome explicitly named in spec.md's SC-001 with zero prior coverage — is now unit-tested, confirming the admin-driven `ResolveDispute` GRACEFUL path (distinct from `AcknowledgePayment`'s own mutual-acknowledgment GRACEFUL path — see FR-006/FR-007): full balance transfer, no penalty, resolves to `ADMIN_RESOLVED`. No bug found. SC-001 corrected in spec.md. L3 `GRACEFUL` coverage remains absent but L1 is sufficient to close the spec-vs-test contradiction that was the actual finding here. |
| FR-010 | `ListPayments` MUST filter to the caller's own bills in the given org, using `show_history` to toggle active vs. completed statuses. | `TestBillSplitService_ListPayments > "Success_ShowHistory"` (`tests/unit/bill_split_service_test.go:80`), `> "Success_NoHistory"` (`:99`), `> "Error_NotMember"` (`:115`) | — | `TestBillSplitService_ListPayments` (`tests/e2e/bill_split_test.go:443`) — asserts active list (`PENDING`+`DISPUTED`) vs. history list (`PAID`) counts against a real DB | — | **Complete** | No L2 test exercises this RPC directly, but the FR's core clause (own-bills scoping + `show_history` toggle) is proven both against the real service interface (L1, including the not-a-member rejection) and end-to-end through the real gRPC/DB stack (L3) — per `sbr/README.md`, not every requirement needs every tier, and L3 here already subsumes what an L2 test would add. |
| FR-011 | `GetPaymentDetail` MUST be restricted to the bill's debtor, creditor, or an org admin, and MUST compute `can_acknowledge` per User Story 2's rules. | `TestBillSplitService_GetPaymentDetail > "Success_AsDebtor"` (`tests/unit/bill_split_service_test.go:140`), `> "Success_AsCreditor"` (`:155`), `> "Error_NotInvolved"` (`:171`), `> "Error_BillNotFound"` (`:184`) | — | Within `TestBillSplitService_PaymentAcknowledgment` (`tests/e2e/bill_split_test.go:52-58`) — debtor view, `CanAcknowledge=true`; within `TestBillSplitService_UnauthorizedAccess` (`:592-595`) — non-member rejection | — | **Gap — L1 (partial), L2, L3 (partial)** | No test anywhere has an **org admin** (non-party) successfully call `GetPaymentDetail` — only debtor/creditor success and a non-member/non-party rejection are covered, leaving one of the three documented authorized-caller classes completely untested. `can_acknowledge = false` branches (e.g. already acknowledged, or ineligible status) are likewise never exercised — only the two `true` cases are asserted. No integration (L2) test calls this RPC at all. |
| FR-012 | `ListDisputedPayments`/`ListResolvedDisputes` MUST be restricted to org admins; `ListDisputedPayments` MUST exclude bills the calling admin is a party to. | `TestBillSplitService_ListDisputedPayments > "Success"` (`tests/unit/bill_split_service_test.go:207`), `> "Error_NotAdmin"` (`:222`); `TestBillSplitService_ListResolvedDisputes > "Success"` (`:243`, no rejection subtest) | `"Dispute Lifecycle"` (`tests/integration/bill_split_test.go:139`) — admin calls `ListDisputedPayments`, admin is not a party in this fixture | `TestBillSplitService_DebtorDisputesPayment` (`tests/e2e/bill_split_test.go:108`) for `ListDisputedPayments`; `TestBillSplitService_ListResolvedDisputes` (`:507`) — admin-caller happy paths only | — | **Gap — L1 (partial), L2 (partial), L3 (partial)** | The admin-restriction clause has a real rejection test only for `ListDisputedPayments` (`Error_NotAdmin`) — `ListResolvedDisputes` has no rejection test at any tier. More significantly, the **exclusion clause itself is never exercised**: in every test (unit, integration, e2e) the admin is a party-free third user, so "excluding any bill where the calling admin is debtor or creditor" is asserted only as a mocked pass-through parameter (`tests/unit/bill_split_service_test.go:213`, `ListDisputedByOrg(..., &adminID)`), never against a real disputed bill where the admin actually IS a party. No test creates that adversarial fixture. |
| FR-013 | As-built, `ListPayments`/`ListDisputedPayments`/`ListResolvedDisputes` MUST return the full unfiltered/unpaginated result set — pagination and `settlement_month`/`resolution_outcome` filters are accepted but ignored. | — | — | — | — | **Gap — all tiers** | `grep -i pagination tests/e2e/bill_split_test.go` and equivalent searches for `SettlementMonth`/`ResolutionOutcome` filter usage return nothing. No test ever populates these request fields and asserts they have no effect — the as-built "ignored" behavior that Known Discrepancy 1 and SC-005 hinge on is entirely unverified by regression test, so a future accidental implementation of real filtering would not be caught as a behavior change. |

## Summary

- **13 FR-IDs audited, 3 fully `Complete`** (FR-006, FR-009, FR-010 — FR-006/FR-009 closed
  2026-07-22 as SBR remediation Phase 1).
- **Remaining gaps**: FR-001 (snapshot/skip-settled), FR-002/FR-003/FR-004/FR-005 (the entire
  automated notice/reminder/dispute-escalation/auto-resolution background-job pipeline — Phase
  2/3), FR-007, FR-008, FR-011, FR-012, FR-013 — tracked in `sbr/remediation-plan.md`.
- **Phase 1 outcome**: FR-006's zero-coverage reject clauses closed (8 new subtests, no bug
  found — the deferral-to-e2e comment's implicit promise was never actually fulfilled by the
  e2e file it pointed to). FR-009's SC-001 spec-vs-test contradiction resolved — the `GRACEFUL`
  outcome of admin-driven `ResolveDispute` (distinct from `AcknowledgePayment`'s own GRACEFUL
  path) now has a dedicated unit test; no bug found. spec.md's SC-001 corrected.
- **Unclassified**: none — every row above was resolved to either cited evidence or an
  explicit, named gap.
