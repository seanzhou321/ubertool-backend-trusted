# Requirements Traceability Matrix — Notifications

- **Source spec**: `specs/004-notifications/spec.md`
- **Tier mapping in effect**: `sbr/README.md` → "This repo's adapter" (L1=`tests/unit`,
  L2=`tests/integration`, L3=`tests/e2e`, Grounding=`tests/smoke`+`tests/e2e` journeys)
- **Generated**: 2026-07-22 by `speckit-sbr-audit`; updated 2026-07-23 (Phase 4 hardening pass)
- **Mode**: retrofit audit (as-built) — see project constitution Principle I

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding (Smoke) | Boundary Status | Notes |
|---|---|---|---|---|---|---|---|
| FR-001 | `GetNotifications` MUST return only the caller's own notifications, with `total_count`, and MUST support `limit`/`offset` semantics as documented. | `TestNotificationService_GetNotifications_NonAlignedOffset` (`tests/unit/notification_service_test.go`) | **Intentionally not separately tested** — see Notes | `TestNotificationService_E2E > "GetNotifications"` (`tests/e2e/notification_test.go:24`); `> "GetNotifications with Pagination"` (`:100`) — now includes a non-page-aligned `offset=12`/`limit=10` case against 15 seeded rows | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3) — real bug found and fixed.** Confirmed spec.md's own Known Discrepancy 1 / SC-001: the gRPC handler computed `page := (offset/limit)+1` via integer division, silently rounding any non-page-aligned offset down to the nearest page boundary (`offset=12` behaved identically to `offset=10`). Fixed by having `NotificationService.GetNotifications` accept a true `limit`/`offset` pair directly instead of round-tripping through page/pageSize; the handler now passes `req.Limit`/`req.Offset` straight through. Regression-locked by both a unit test and a new e2e subtest that would have failed under the old logic. L2 not added (2026-07-23 hardening pass): the e2e pagination subtest already exercises the repository's `LIMIT`/`OFFSET` SQL directly against real Postgres with 15 seeded rows — L3 subsumes L2 here. |
| FR-002 | `MarkNotificationRead` MUST be idempotent (first-write-wins on `read_at`) and MUST reject a `notification_id` that does not exist or does not belong to the caller. | — | `TestNotificationRepository_MarkAsRead_Idempotent`, `TestNotificationRepository_MarkAsRead_RejectsWrongOwner` (`tests/integration/notification_test.go`) — real DB | `TestNotificationService_E2E > "MarkNotificationRead"` (`tests/e2e/notification_test.go:67`); `> "MarkNotificationRead - Wrong User"` (`:143`) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** The idempotency clause is a DB-level `COALESCE(read_at, NOW())` semantic, so it needed a real-DB integration test: confirms a second `MarkAsRead` call on an already-read notification does not change `read_at`. No bug found. |
| FR-003 | `Dispatch` MUST persist the notification before attempting any push send, and MUST NOT let a push-delivery failure surface as a `Dispatch` error. | `TestNotificationService_Dispatch` (`tests/unit/notification_service_test.go`) — 2 subtests: persists + swallows a push-send failure, propagates a persistence failure without attempting a push | `TestNotificationService_Dispatch_Integration` (`tests/integration/notification_test.go`) — real DB: confirms the notification row is actually persisted despite a simulated push-send failure, independent of live Firebase | `TestPushNotificationService_E2E > "DispatchToUser1_ReceivesPush"` / `"DispatchToUser2_ReceivesPush"` (`tests/e2e/push_notification_test.go:101,139`) — happy path (depends on live Firebase device tokens; see this file's environment note) | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2, L1).** First unit test ever written for `notificationService`. Confirms `Dispatch` returns no error even when the injected push service returns one, and that persistence failure is propagated without a push attempt. No bug found. **L2 added 2026-07-23 (hardening pass)** to give this guarantee real-DB, Firebase-independent evidence — the only prior non-L1 evidence (L3) depends on live device tokens that are documented elsewhere as having gone stale in this environment. No additional bug found. |
| FR-004 | Every push send MUST embed the originating `notification_id` in the FCM data payload. | `TestPushSvc_SendToUser_PayloadContainsNotificationID` (`tests/unit/push_notification_service_test.go:132`) — asserts `msg.Data["notification_id"]` matches the passed ID | **N/A** — see Notes | **N/A** — see Notes | — | **Complete** | Direct, unambiguous unit-level assertion on the payload construction — the strongest possible evidence for a pure data-shape requirement. No additional tier needed: this is a static struct-field-construction check with no distinct real-DB or contract-level behavior for L2/L3 to add. |
| FR-005 | A token failing with `Unregistered` or `InvalidArgument` MUST be marked `OBSOLETE` and MUST NOT be retried; any other failure MUST be retried up to 3 times with back-off before being dropped. | `TestPushSvc_SendAsync_UnregisteredError_MarksObsolete_NoRetry`, `TestPushSvc_SendAsync_UnregisteredError_MarkObsoleteFails_NoRetry`, `TestPushSvc_SendAsync_TransientThenUnregistered_StopsAtUnregistered`, `TestPushSvc_SendAsync_AllRetriesExhausted`, `TestPushSvc_RetryAttemptOrder`, `TestPushSvc_SendToUser_InvalidArgumentError_MarksObsolete_NoRetry` (all `tests/unit/push_notification_service_test.go`) — cover both the `Unregistered` and `InvalidArgument` branches plus the transient-retry-count branch | **Intentionally not separately tested** — see Notes | `TestPushNotificationService_E2E > "UnregisteredToken_MarkedObsolete"` (`tests/e2e/push_notification_test.go:218`) — real Firebase UNREGISTERED response via a deliberately dead token, triggered through a real `Dispatch` call | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3).** The `InvalidArgument` branch was a hardcoded `messaging.IsInvalidArgument(sendErr)` call that no synthetic test error could trigger (it only recognizes a real `*internal.FirebaseError`, a type outside this module's importable tree). Fixed the same way as `isUnregisteredFn`: added an injectable `isInvalidArgumentFn` field (default `messaging.IsInvalidArgument` in production) plus a `SetInvalidArgumentFnForTest` setter — a testability fix, not a behavior change. No bug found; the retry-count/back-off-ordering clauses remain tested for count/order only, not literal delay magnitudes, which is the behaviorally load-bearing part. L2 not added (2026-07-23 hardening pass): the cited e2e test already marks a real FCM token row OBSOLETE against Postgres via a deliberately-dead token (independent of live device availability) — L3 subsumes L2 here. |
| FR-006 | `SyncDeviceToken` MUST upsert on the FCM token's own uniqueness, reassigning `user_id` when the same token re-registers under a different user. | `TestNotificationService_SyncDeviceToken` (`tests/unit/notification_service_test.go`) — confirms the service constructs the `FcmToken` correctly (ACTIVE status, `device_name` mapped into `DeviceInfo`) before upserting | `TestFcmTokenRepository_Upsert_Reassignment` (`tests/integration/fcm_token_test.go`) — real DB: user A registers a token, then user B re-registers the *same* token; asserts single-row reassignment (no duplicate, user A no longer sees it) | `TestPushNotificationService_E2E > "SyncDeviceToken_User1_Idempotent"` / `"SyncDeviceToken_User2_Idempotent"` (`tests/e2e/push_notification_test.go:185,202`) — same-user re-sync produces no duplicate row | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2, L2).** The reassignment clause is a DB-level `ON CONFLICT (fcm_token) DO UPDATE` semantic, so it needed a real-DB integration test rather than a mock. No bug found. **L1 added 2026-07-23 (hardening pass):** the service's own parameter-mapping logic (distinct from the repo's DB-level reassignment semantic already covered at L2) had zero coverage at any tier. No bug found. |
| FR-007 | `ReportMessageEvent` MUST reject an `event_type` outside `{DELIVERED, CLICKED}`. (As-built, MUST NOT be assumed to reject an invalid/foreign `notification_id` — spec.md's Known Discrepancy 2.) | `TestNotificationService_ReportMessageEvent` (`tests/unit/notification_service_test.go`) — 3 subtests: routes DELIVERED, routes CLICKED, rejects any other event_type | `TestNotificationRepository_ReportMessageEvent_Idempotent` (`tests/integration/notification_test.go`) — real DB, 2 subtests: `DELIVERED` is first-write-wins (`COALESCE`), `CLICKED` scoped to the wrong `user_id` does not stamp the row | — | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 3, L1).** First real invocation of this RPC anywhere in the test suite (previously only inert mock-interface stubs existed). No bug found. **L2 added 2026-07-23 (hardening pass):** `MarkDelivered`/`MarkClicked` are DB-level `COALESCE(...)` first-write-wins semantics (the same pattern as FR-002's `MarkNotificationRead`, already L2-tested) that had zero real-DB coverage. No bug found. |
| FR-008 | `SendMulticastToUsers` MUST batch tokens into groups of at most 500 per Firebase multicast call and MUST mark `OBSOLETE` any token reported unregistered in a batch response. | `TestPushSvc_SendMulticastToUsers_BatchesAt500`, `TestPushSvc_SendMulticastToUsers_MarksUnregisteredTokensObsolete`, `TestPushSvc_SendMulticastToUsers_NoActiveTokens_NoOp` (`tests/unit/push_notification_service_test.go`) | `TestFcmTokenRepository_GetActiveByUserIDs` (`tests/integration/fcm_token_test.go`) — real DB: returns only ACTIVE/TESTING tokens across a 3-user set, excluding one explicitly marked OBSOLETE | — | — | **Complete** | **Fixed 2026-07-23 (SBR remediation Phase 2, L1).** Test-mode construction previously left `fcmMulticastClient` nil, structurally foreclosing this path; added `SetMulticastClientForTest` plus a `MockFCMMulticastSender`. Also refactored the obsolete-marking check from a hardcoded `messaging.IsUnregistered` call to the already-injectable `s.isUnregisteredFn` (same pattern used elsewhere in this file) so synthetic test errors can be recognized — a testability fix, not a behavior change (production still defaults to `messaging.IsUnregistered`). 600 tokens confirmed split into 500+100 batches; unregistered tokens confirmed marked OBSOLETE without touching successfully-delivered ones. No bug found. **L2 added 2026-07-23 (hardening pass):** `GetActiveByUserIDs` — the actual SQL query this RPC's batching logic depends on — had zero real-DB coverage anywhere (only ever mocked). No bug found. L3 not added: this RPC fires its real work in a background goroutine with no synchronous, pollable e2e signal distinct from what the L1 batching tests and this new L2 query test already establish. |

## Summary

- **8 FR-IDs audited, 8 of 8 fully `Complete`** (FR-004 previously; FR-003, FR-006, FR-008
  closed 2026-07-23 as SBR remediation Phase 2; FR-001, FR-002, FR-005, FR-007 closed
  2026-07-23 as Phase 3).
- **Remaining gaps**: none tracked for this feature — all FRs are `Complete`.
- **Phase 2 outcome**: FR-003's error-containment guarantee unit-tested (first-ever unit test
  for `notificationService`); FR-006's `user_id`-reassignment clause closed with a real-DB
  integration test (the clause is a DB-level `ON CONFLICT` semantic, not mockable); FR-008's
  batching and obsolete-marking closed by adding test-mode multicast-client injection and
  refactoring the obsolete check to use the already-injectable `isUnregisteredFn`. No bugs found
  in any of the three.
- **Phase 3 outcome**: FR-001's pagination gap turned out to be a **real, previously-masked
  bug** — the gRPC handler's `page := (offset/limit)+1` integer-division round-trip silently
  rounded non-page-aligned offsets to the nearest page boundary, exactly as spec.md's own Known
  Discrepancy 1 / SC-001 described. Fixed by having the service accept a true `limit`/`offset`
  pair directly; regression-locked by both a unit test and a new e2e subtest that fails under
  the old logic. FR-002's idempotency clause closed with a real-DB integration test (DB-level
  `COALESCE` semantic). FR-005's `InvalidArgument` branch closed via the same injectable-predicate
  pattern already used for `isUnregisteredFn`. FR-007 (`ReportMessageEvent`) went from zero
  coverage (mock-interface stubs only) to a real unit test. No further bugs found.
- **Phase 4 hardening pass (2026-07-23)**: re-audited every cell for unexplained blanks. Closed
  4 genuine gaps with new tests: FR-003's `Dispatch` error-containment guarantee now has a
  Firebase-independent real-DB test (its only prior non-L1 evidence depends on live device
  tokens documented elsewhere as stale in this environment); FR-006's `SyncDeviceToken` service-
  level parameter mapping (distinct from the DB reassignment semantic already at L2) closed at
  L1; FR-007's `MarkDelivered`/`MarkClicked` DB-level first-write-wins semantics closed at L2
  (same pattern as FR-002's `MarkNotificationRead`); FR-008's `GetActiveByUserIDs` — the actual
  SQL query the multicast batching logic depends on, previously only ever mocked — closed at L2.
  Remaining blanks documented as legitimate: FR-001/FR-005 (L3 already subsumes L2 against real
  Postgres), FR-004 (pure data-shape, no L2/L3 boundary exists). No bugs found in any of the 4
  new tests.
- **Unclassified**: none — every row above was resolved to either cited evidence or an explicit,
  named gap.
