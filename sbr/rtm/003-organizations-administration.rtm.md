# SBR Requirements Traceability Matrix: 003-organizations-administration

**Source Spec**: `specs/003-organizations-administration/spec.md`
**Adapter**: `sbr/README.md` (This repo's adapter)
**Generated**: 2026-07-29 (full re-audit — see "2026-07-29 re-audit note" below)
**Mode**: retrofit audit

| FR-ID | Requirement Summary | L1 Unit | L2 Integration | L3 E2E | Grounding | Boundary Status | Planned Tests | Notes |
|---|---|---|---|---|---|---|---|---|
| FR-001 | `CreateOrganization` requires no pre-existing role beyond authentication (when the API-creation flag is on) — caller becomes `SUPER_ADMIN`, `balance_cents = 0`. | `TestOrganizationService_CreateOrganization` (tests/unit/org_service_test.go:77) | — | `TestOrganizationService_E2E` > "CreateOrganization with SUPER_ADMIN Assignment" (tests/e2e/org_test.go:23) | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Complete | — | Flag-enabled path verified at unit and e2e. No dedicated L2 test creates an org independently of other fixtures; L1+L3 judged sufficient for this CRUD+role-assignment shape. |
| FR-001a *(added 2026-07-29)* | When `features.allow_api_organization_creation` is `false`, `OrganizationHandler.CreateOrganization` MUST reject every caller with `codes.PermissionDenied` before calling the service layer. | — | — | — | — | Gap — L1, L2, L3 | L1: `TestOrganizationHandler_CreateOrganization_RejectsWhenAPICreationDisabled` (new, tests/unit/handlers/) — construct `OrganizationHandler` with `allowAPIOrgCreation=false`, assert `codes.PermissionDenied` and that `orgSvc.CreateOrganization` is never called. L3: `TestOrganizationService_E2E` > "CreateOrganization rejected when API creation is disabled" — needs a server run against a config with the flag `false` (the current e2e suite only runs under `config/config.precommit.yaml`, which has it `true`; would need a second e2e config or a table-driven server-config variant). No L2 needed — this is a pure handler-level gate with no DB interaction. | Newly added alongside the config flag (`internal/config/config.go`, enforced in `internal/api/grpc/org.go:CreateOrganization`). The flag-true path is only implicitly covered by e2e running under `config/config.precommit.yaml`. |
| FR-002 | `UpdateOrganization` rejects non-members, requires `ADMIN`/`SUPER_ADMIN` for any change, and requires `SUPER_ADMIN` specifically for either price-threshold field (`0` = no change). | `TestOrganizationService_UpdateOrganization` (tests/unit/org_service_test.go:103) — 7 subtests covering member/role/price-field branches and the "0 means no change" semantics | — | `TestOrganizationService_E2E` > "UpdateOrganization by SUPER_ADMIN updates fields and price thresholds" (tests/e2e/org_test.go:56) | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Complete | — | 7-subtest unit suite regression-locks every role/member/price-field branch; e2e confirms one full happy path through the real stack. No L2. |
| FR-003 | `UpdateOrganization` MUST broadcast (in-app notification + email + push) to every active member when either price threshold actually changes. | `TestOrganizationService_UpdateOrganization_ThresholdBroadcast` (tests/unit/org_service_test.go:222) — "Broadcasts to active members...", "Does not broadcast when no threshold field actually changes" | — | Same subtest as FR-002 (tests/e2e/org_test.go:56, commented `// FR-003`) — polls for the in-app notification only | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Complete | — | L1 asserts all three channels (`DispatchSilent`, `SendAdminNotification` email, `SendMulticastToUsers` push) plus blocked-member exclusion from the multicast. L3 only confirms the in-app notification channel end-to-end — email/push aren't independently asserted at e2e (mocked/no-op under the precommit config). |
| FR-004 | `JoinOrganizationWithInvite` validates invite-email ownership, rejects an already-member caller, and on success notifies all org admins. | `TestOrganizationService_JoinOrganizationWithInvite` (tests/unit/org_service_test.go:304) — "Success adds the caller as MEMBER", "Rejects when the caller is already a member", "Rejects an invitation issued to a different email", "Rejects an already-used invitation" | — | `TestOrganizationService_E2E` > "JoinOrganizationWithInvite" (tests/e2e/org_test.go:106) | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Gap — admin-notification fan-out | L1: `TestOrganizationService_JoinOrganizationWithInvite_NotifiesOrgAdmins` (new, tests/unit/org_service_test.go) — seed 2 ADMIN/SUPER_ADMIN members in `ListMembersByOrg`, assert `noteRepo.DispatchSilent` (and email/push, mirroring the FR-003 broadcast test's pattern) is called once per admin on a successful join. | Invite-validation and membership logic are fully covered at L1+L3. The "notify all org admins" half of FR-004 is untested at every tier — no test asserts a notification is dispatched on a successful join (`internal/service/org.go:360`'s "New Member Joined" notification has no covering test). spec.md's own "Not covered anywhere" list already self-flags this. |
| FR-005 | Every `AdminService` RPC (8 total) MUST require `ADMIN`/`SUPER_ADMIN` membership in the target org, checked before any other work. | `TestAdminService_RequiresAdminRole` (tests/unit/admin_service_test.go:243) — one rejection subtest per RPC (8) plus "no membership at all" | — | `TestAdminService_E2E` > "AdminBlockUserAccount rejects a non-admin caller" (tests/e2e/admin_test.go:248) | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Complete | — | L1 proves the authorization guard on all eight RPCs. L3 confirms only 1 of 8 (`AdminBlockUserAccount`) through the real gRPC/DB stack — spec.md's own SC-001 documents this as the accepted current scope, not a hidden gap. No L2. |
| FR-006 | `RejectRequestToJoin` MUST expire any invitation already linked to the rejected join request. | `TestAdminService_RejectJoinRequest_ExpiresLinkedInvitation` (tests/unit/admin_service_test.go:398) — "Expires a linked, not-yet-used invitation", "Does not touch an invitation that has already been used" | — | `TestAdminService_E2E` > "RejectRequestToJoin expires the linked invitation" (tests/e2e/admin_test.go:113) | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Complete | — | — |
| FR-007 | `ListMyOrganizations` MUST return every org the caller belongs to, each with member count and the caller's own role/balance. | `TestOrganizationService_ListMyOrganizations` (tests/unit/org_service_test.go:394) — "Returns every org with its member count and the caller's own role/balance", "Skips a membership whose org record can't be loaded, returning the rest", "Returns an empty list when the caller belongs to no organizations" | `TestOrganizationService_ListMyOrganizations_MultiOrgMembership` (tests/integration/org_test.go:18) | `TestOrganizationService_E2E` > "ListMyOrganizations" (tests/e2e/org_test.go:134) | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Complete | — | Covered at all three tiers, including partial-failure (one org record fails to load) at L1. |
| FR-008 | `ListJoinRequests` (auth covered by FR-005) MUST return every `join_requests` row for the org — as-built, MUST NOT be assumed to filter by `status = 'PENDING'` (Known Discrepancy 3). | `TestAdminService_RequiresAdminRole` > "ListJoinRequests rejects a plain MEMBER caller" (tests/unit/admin_service_test.go:302) — auth only | `TestAdminService_ListJoinRequests_UsedOnField` (tests/integration/admin_join_request_test.go:17) — tests `used_on` field population | `TestAdminService_E2E` > "ListJoinRequests" (tests/e2e/admin_test.go:216) | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Gap — status-independence unverified | L2: `TestAdminService_ListJoinRequests_ReturnsNonPendingRequests` (new, tests/integration/admin_join_request_test.go) — seed one `APPROVED` and one `REJECTED` join_request alongside a `PENDING` one, assert `ListJoinRequests` returns all three. | Authorization and `used_on`-population are genuinely tested. But the Known-Discrepancy-3 claim itself ("returns rows regardless of status") has **no real test evidence**: both the L2 test (admin_join_request_test.go:65-73) and the L3 test (admin_test.go:224-227) seed only `status = 'PENDING'` join_requests. **spec.md's FR-008 text is factually wrong about its own cited evidence** — it says the L2 test "creates two join requests with `status = 'APPROVED'`"; the test actually inserts two `'PENDING'` rows. Recommend either correcting spec.md's claim or adding the planned test. |
| FR-009 *(multi-org, PRD 3.1)* | Users MUST be able to belong to multiple organizations simultaneously (`users_orgs` composite PK `(user_id, org_id)`), independently per org. | — | `TestOrganizationService_ListMyOrganizations_MultiOrgMembership` (tests/integration/org_test.go:18) | — | `TestDatabaseConnectivity` > "OrgsTableReachable_via_SearchOrganizations" (tests/smoke/smoke_test.go:124) | Complete | — | Cross-org independence is directly verified at L2 against a real Postgres instance — the tier best suited to a DB-schema-level guarantee. No dedicated L1/L3 needed for this shape. |
| FR-010 *(multi-org, PRD 3.1, 3.3)* | The system MUST support a server-side "current organization" context for the caller, used as the default scope for search/rentals/billing/ledger. | — | — | — | — | Gap — L1, L2, L3 | N/A — requirement needs correction (or removal), not a test. This functionality does not exist in the codebase and was deliberately removed (see Notes) — writing a test against a mechanism that doesn't exist and isn't planned would have nothing to assert. | **This requirement describes functionality that does not exist in the codebase.** `grep` for `SetCurrentOrganization`, `GetCurrentOrganization`, and related terms across `internal/`, `tests/`, and `api/proto/` returns zero results. `specs/003-organizations-administration/contracts/README.md`'s "Correction 2026-07-28" note already documents that this design was implemented then removed — "current organization" is a client/device-local UI preference, not server state. **spec.md's FR-010 is stale and should be corrected or removed**, not just re-audited — flagged for human review as a confident negative, not a search failure. |
| FR-011 *(multi-org, PRD 3.3)* | Cross-organization tool search MUST include tools from ALL orgs the caller belongs to in the same/compatible metro, using the "current organization's" metro as primary filter. | — | — | — | — | Gap — L1, L2, L3 *(as literally worded)* | N/A — requirement needs correction, not a test. The literal "current organization context" mechanism this FR describes doesn't exist (see FR-010); the real, testable capability already has its own tests under a different, accurate requirement — see 006-tools-image-storage FR-008. | The underlying **capability** — cross-org tool search in a shared metro — is implemented and tested, but via an explicit `organization_id` request parameter plus a shared-organization post-filter, not the "current organization context" mechanism this FR describes. The accurate, current specification of this capability is `specs/006-tools-image-storage/spec.md` FR-008/FR-009 (see `sbr/rtm/006-tools-image-storage.rtm.md` for its test evidence). Citing 006's tests here would misattribute evidence to a requirement worded around a nonexistent mechanism — flagged for spec.md correction rather than a false `Complete`. |
| FR-012 *(multi-org, PRD 3.3, UI-Design 10.3)* | Renting a tool owned by a member of a different (shared) org MUST prompt the user for an organization-context switch before proceeding; last-selected org per session MUST be preserved. | — | — | — | — | Gap — L1, L2, L3 | N/A — requirement needs correction (or removal), not a test. Same reasoning as FR-010: the described flow doesn't exist and isn't planned; the real, corrected design (validated directly inside `CreateRentalRequest`) already has its own tests under `specs/005-rentals/spec.md` FR-008. | **No context-switch-prompt flow, or session-scoped "last selected organization," exists anywhere in the repo.** Removed per the same 2026-07-28 correction as FR-010. The corrected design (`docs/design/multi-org.md`, `specs/005-rentals/spec.md` FR-008) validates the shared-org relationship directly inside `CreateRentalRequest` via an explicit `organization_id` argument — no client-side context-switch step. **spec.md's FR-012 is stale and should be corrected or removed.** |

## Summary
- Total FRs: 13 (FR-001 through FR-012, plus FR-001a added 2026-07-29)
- Complete: 7 (FR-001, FR-002, FR-003, FR-005, FR-006, FR-007, FR-009)
- Gaps: 6 (FR-001a, FR-004 — partial, FR-008 — partial, FR-010, FR-011, FR-012)
- Unclassified: 0

**Gaps**: FR-001a (no test at any tier — planned test proposed), FR-004 (admin-notification fan-out untested — planned test proposed), FR-008 (status-independence claim unverified, spec.md misdescribes its own cited evidence — planned test proposed), FR-010/FR-011/FR-012 (describe removed/nonexistent or misworded functionality — spec.md correction needed before any test is meaningful, not a test-writing gap).

## 2026-07-29 re-audit note

This file replaces a 2026-07-27-generated version whose test citations were, on inspection, **systematically wrong** — e.g. FR-002 (`UpdateOrganization`) cited `TestAdminService_BlockUser`, and FR-001 (`CreateOrganization`) cited `TestAdminService_BlockUser_RankCheck`; neither test touches the requirement it was cited for. These were not forward-looking placeholders for tests-to-be-written (the cited tests already exist, pass, and belong to a different feature — Admin authorization — not to a not-yet-implemented Organizations test), so they were replaced with independently re-verified evidence rather than treated as a TDD plan. This version adds a **Planned Tests** column (see `sbr/README.md`'s "RTM schema" section) to make the forward-looking test-pyramid plan explicit and separate from backward-looking evidence, per 2026-07-29 discussion. Given the same 2026-07-27 generation process likely produced the other seven feature RTMs and the security RTM, those should be treated as unverified until similarly re-audited — this pass only covers 003, plus the four other specs touched by the 2026-07-28 multi-org work (005, 006, 007, 008; see their own RTM files).
